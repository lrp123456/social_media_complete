# 抖音 0s / 设置页 #310 / 快手登录链路 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复抖音复用标签页 0s 假成功、系统设置页 React #310、快手登录检测（HAR）并统一四平台"同标签页登录"链路。

**Architecture:** 三处独立修复 + 一处链路重构：(1) `navigateToCreatorHome` 跳过条件与入口对齐 + fast-fail 改抛错；(2) `MaterialTab` hook 上移；(3) 快手 `detectKuaishouLoginV2` 用网络层 `account/current` 拦截为主、DOM 智能轮询兜底；(4) `sendLoginQR` 重构为 `performLoginOnCurrentTab`，全程不新建标签页、截图永远用当前 page。

**Tech Stack:** TypeScript, Playwright/Patchright CDP, RequestInterceptor, React/Next, Jest/Vitest.

---

## File Structure

| 文件 | 职责 |
|---|---|
| `apps/ts-api-gateway/src/crawlers/douyinCrawler.ts` | `navigateToCreatorHome` 跳过条件对齐；抽 `isOnDouyinWorkPage`；`performLoginOnCurrentTab` 适配 |
| `apps/ts-api-gateway/src/crawlers/kuaishouCrawler.ts` | `detectKuaishouLoginV2`（HAR+DOM）；`navigateToHome` 修 about:blank；`handleLogin` 移除阻塞循环；点击"立即登录" |
| `apps/ts-api-gateway/src/crawlers/tencentCrawler.ts` | `performLoginOnCurrentTab` 适配 |
| `apps/ts-api-gateway/src/crawlers/xiaohongshuCrawler.ts` | `performLoginOnCurrentTab` 适配 |
| `apps/ts-api-gateway/src/services/monitorService.ts` | `runDouyinCheck`/`runKuaishouCheck` fast-fail 改抛错；`sendLoginQR` → `performLoginOnCurrentTab`、移除 fallback；Phase0 用 V2 |
| `apps/ts-api-gateway/src/services/wechatBotService.ts` | "继续监控"指令用 `detectKuaishouLoginV2` |
| `apps/admin-dashboard/src/app/settings/tabs/MaterialTab.tsx` | `useMaterialDiskUsage` 上移 |
| `apps/ts-api-gateway/src/services/__tests__/*.test.ts` | 各 task 配套测试 |

---

### Task 1: 抖音 navigateToCreatorHome 跳过条件对齐 + isOnDouyinWorkPage 抽取

**Files:**
- Modify: `apps/ts-api-gateway/src/crawlers/douyinCrawler.ts:193-223`
- Test: `apps/ts-api-gateway/src/services/__tests__/navigateToCreatorHome.test.ts`

**背景**：`navigateToCreatorHome` 的 `currentUrl.includes('creator.douyin.com')` 跳过条件与 `runDouyinCheck` 入口 `onWorkPage`（`/creator-micro/home` 或 `/creator-micro/content/manage`）不一致，复用标签页残留子页时被跳过导致 0s。

- [ ] **Step 1: 写失败测试**

```typescript
import { describe, it, expect } from '@jest/globals';

// isOnDouyinWorkPage 作为纯函数从模块导出
import { isOnDouyinWorkPage } from '../crawlers/douyinCrawler';

describe('isOnDouyinWorkPage', () => {
  it('/creator-micro/home → true', () => {
    expect(isOnDouyinWorkPage('https://creator.douyin.com/creator-micro/home')).toBe(true);
  });
  it('/creator-micro/content/manage → true', () => {
    expect(isOnDouyinWorkPage('https://creator.douyin.com/creator-micro/content/manage')).toBe(true);
  });
  it('/creator-micro/data/following/follower 残留子页 → false', () => {
    expect(isOnDouyinWorkPage('https://creator.douyin.com/creator-micro/data/following/follower')).toBe(false);
  });
  it('about:blank → false', () => {
    expect(isOnDouyinWorkPage('about:blank')).toBe(false);
  });
  it('仅 creator.douyin.com 域但无工作路径 → false', () => {
    expect(isOnDouyinWorkPage('https://creator.douyin.com/')).toBe(false);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd apps/ts-api-gateway && npx jest src/services/__tests__/navigateToCreatorHome.test.ts`
Expected: FAIL — `isOnDouyinWorkPage` 未定义/未导出

- [ ] **Step 3: 实现 isOnDouyinWorkPage 并改造 navigateToHome**

在 `douyinCrawler.ts` 文件顶部（常量区附近）新增导出函数：

```typescript
/** 抖音工作页判定：作品首页或作品管理页。供 runDouyinCheck 入口与 navigateToCreatorHome 共用，避免跳过条件漂移。 */
export function isOnDouyinWorkPage(url: string): boolean {
  return url.includes('/creator-micro/home') || url.includes('/creator-micro/content/manage');
}
```

修改 `navigateToCreatorHome`（`:193-223`），把 `if (currentUrl.includes('creator.douyin.com'))` 改为：

```typescript
async navigateToCreatorHome(page: Page): Promise<void> {
  MaintenanceProbe.enterStep('monitor', 'douyin', 'Phase1', 'navigateToCreatorHome');
  try {
    const currentUrl = page.url();

    if (isOnDouyinWorkPage(currentUrl)) {
      logger.info({ currentUrl }, 'Already on douyin work page, skipping navigation');
    } else {
      logger.info({ currentUrl }, 'Navigating to douyin creator home via click-based menu');
      // 尝试点击"创作者服务平台"链接（防风控）, 失败时回退到 goto
      const clicked = await resolveAndClick(page, 'nav.to-creator', 'douyin', { timeout: 10000 });
      if (clicked) {
        await HumanActions.wait(page, 2000, 4000);
        await HumanActions.pageLoadBehavior(page);
      } else {
        logger.warn('Click-based nav to creator failed, falling back to page.goto');
        await page.goto(CREATOR_HOME, { waitUntil: 'domcontentloaded' });
        HumanActions.clearCDPContext(page);
        await HumanActions.wait(page, 2000, 4000);
        await HumanActions.pageLoadBehavior(page);
      }
    }

    this.currentMenuSection = 'unknown';
    await BrowserManager.logPageHtml(page, 'after_navigateToCreatorHome');
    logger.info({ currentUrl: page.url() }, 'Ready on creator page');
  } finally {
    MaintenanceProbe.exitStep();
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd apps/ts-api-gateway && npx jest src/services/__tests__/navigateToCreatorHome.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add apps/ts-api-gateway/src/crawlers/douyinCrawler.ts apps/ts-api-gateway/src/services/__tests__/navigateToCreatorHome.test.ts
git commit -m "fix(抖音): navigateToCreatorHome 跳过条件改用 isOnDouyinWorkPage，对齐入口判定"
```

---

### Task 2: runDouyinCheck/runKuaishouCheck fast-fail 改抛错

**Files:**
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts:1077-1082`（抖音）、`:1274-1278`（快手）
- Test: `apps/ts-api-gateway/src/services/__tests__/runDouyinCheckFastFail.test.ts`

**背景**：fast-fail 当前 `return` 正常 `MonitorResult`，被 `handleJob` 记 success 0s。改为抛错 → `executeMonitorCheck` catch 向上抛 → `handleJob` 标 failed → BullMQ 重试。

- [ ] **Step 1: 写失败测试**

```typescript
import { describe, it, expect, jest } from '@jest/globals';

// 复用 runDouyinCheckEntry.test.ts 的 mock 套路；这里聚焦验证导航后仍偏离时抛错
jest.mock('@social-media/shared-config', () => ({
  isProduction: jest.fn().mockReturnValue(false),
  isDevelopment: jest.fn().mockReturnValue(false),
  getConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  loadConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  PlatformName: 'douyin',
}));
jest.mock('../../lib/logger', () => ({
  createLogger: () => ({ error: jest.fn(), warn: jest.fn(), info: jest.fn(), debug: jest.fn() }),
}));
jest.mock('../loginFlowHelpers', () => ({
  getLoginFlowConfig: jest.fn().mockReturnValue(null),
  ensureLoginTab: jest.fn(),
  getFlowIdsForPlatform: jest.fn().mockReturnValue(['creator']),
  activatePlatformQR: jest.fn(),
}));
jest.mock('@social-media/browser-core', () => ({
  RequestInterceptor: jest.fn().mockImplementation(() => ({
    register: jest.fn().mockResolvedValue('pid'),
    unregister: jest.fn(),
    clearAll: jest.fn(),
    setValidationConfig: jest.fn(),
    waitForResponse: jest.fn().mockResolvedValue(null),
    getLatestResponse: jest.fn().mockReturnValue(null),
    getResponses: jest.fn().mockReturnValue([]),
    getRejectionLog: jest.fn().mockReturnValue([]),
  })),
  ExitStrategy: { getQuerySource: jest.fn().mockReturnValue('work_list') },
  HumanActions: { cdpIsElementVisible: jest.fn().mockResolvedValue(true), wait: jest.fn() },
  loginTabRegistry: { find: jest.fn(), register: jest.fn(), captureQR: jest.fn().mockResolvedValue(null), openLoginTab: jest.fn() },
}));
jest.mock('../../routes/config-automation', () => ({ getCrawlConfig: jest.fn().mockResolvedValue({ mode: 'simple', maxRootComments: 50 }) }));

import { runDouyinCheck } from '../monitorService';

describe('runDouyinCheck fast-fail 抛错', () => {
  it('导航后仍不在工作页 → 抛错（不再 return success）', async () => {
    const page = {
      url: jest.fn().mockReturnValue('https://creator.douyin.com/creator-micro/data/following/follower'),
    };
    const dy = {
      navigateToCreatorHome: jest.fn().mockImplementation(async (p: any) => {
        // 模拟导航"失败"：URL 仍是残留子页
        p.url = jest.fn().mockReturnValue('https://creator.douyin.com/creator-micro/data/following/follower');
      }),
      registerListener: jest.fn(),
      unregisterListener: jest.fn(),
    };
    // getDouyinCrawler 返回上述 mock
    jest.doMock('../monitorService', () => {
      const actual: any = jest.requireActual('../monitorService');
      return { ...actual, runDouyinCheck: actual.runDouyinCheck };
    });

    await expect(
      runDouyinCheck(page as any, { userId: 1, platform: 'douyin', windowId: 'w1' } as any, undefined as any),
    ).rejects.toThrow(/偏离工作页|fast-fail|导航/);
  });
});
```

> 注：本测试依赖 `runDouyinCheck` 通过 `getDouyinCrawler(windowId)` 取 crawler。若该间接依赖 mock 困难，可在 task 实现时把 `getDouyinCrawler` 也 `jest.mock` 掉，返回上面的 `dy` 对象。测试目标只有一个：**导航后仍偏离工作页时抛错而非返回正常 result**。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd apps/ts-api-gateway && npx jest src/services/__tests__/runDouyinCheckFastFail.test.ts`
Expected: FAIL — 当前 fast-fail 是 return 不抛错

- [ ] **Step 3: 改抖音 fast-fail 为抛错**

`monitorService.ts:1077-1082`，原：

```typescript
  const dyUrl = page.url();
  const dyOk = dyUrl.includes('/creator-micro/home') || dyUrl.includes('/creator-micro/content/manage');
  if (!dyOk) {
    logger.warn({ userId: task.userId, url: dyUrl }, '[抖音] Phase1 入口 page 偏离作品管理页，fast-fail');
    return { hasUpdate: false, newComments: 0, updatedVideos: [], phase: 'Phase1', riskDetected: false };
  }
```

改为：

```typescript
  const dyUrl = page.url();
  const dyOk = dyUrl.includes('/creator-micro/home') || dyUrl.includes('/creator-micro/content/manage');
  if (!dyOk) {
    logger.error({ userId: task.userId, url: dyUrl }, '[抖音] Phase1 入口导航后仍偏离工作页，抛错触发 failed 重试');
    throw new Error(`[抖音] 入口导航后仍偏离工作页: ${dyUrl}`);
  }
```

- [ ] **Step 4: 改快手 fast-fail 为抛错**

`monitorService.ts:1274-1278`，原：

```typescript
  const ksUrl = page.url();
  if (!ksUrl.includes('/article/publish/video')) {
    logger.warn({ userId: task.userId, url: ksUrl }, '[快手] Phase1 入口 page 偏离视频管理页，fast-fail');
    return { hasUpdate: false, newComments: 0, updatedVideos: [], phase: 'Phase1', riskDetected: false };
  }
```

改为：

```typescript
  const ksUrl = page.url();
  if (!ksUrl.includes('/article/publish/video')) {
    logger.error({ userId: task.userId, url: ksUrl }, '[快手] Phase1 入口导航后仍偏离视频管理页，抛错触发 failed 重试');
    throw new Error(`[快手] 入口导航后仍偏离视频管理页: ${ksUrl}`);
  }
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd apps/ts-api-gateway && npx jest src/services/__tests__/runDouyinCheckFastFail.test.ts`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts apps/ts-api-gateway/src/services/__tests__/runDouyinCheckFastFail.test.ts
git commit -m "fix(监控): 入口 fast-fail 改抛错触发 BullMQ 重试，消灭 0s 假成功"
```

---

### Task 3: 系统设置 #310 — MaterialTab useMaterialDiskUsage 上移

**Files:**
- Modify: `apps/admin-dashboard/src/app/settings/tabs/MaterialTab.tsx:40-115`
- Test: 手动验证（无单测套件覆盖此 tab）

**背景**：`useMaterialDiskUsage()` 在三个 early return 之后调用，hook 数量在渲染间变化触发 React #310。

- [ ] **Step 1: 调整 hook 顺序**

`MaterialTab.tsx:40-58` 当前：

```tsx
export default function MaterialTab() {
  const configQuery = useMaterialConfig();
  const updateConfig = useUpdateMaterialConfig();
  const testPlatform = useTestPlatform();
  const triggerRun = useTriggerMaterialRun();
  const statusQuery = useMaterialStatus();
  const candidatesQuery = useMaterialCandidates(1, 12);

  const [form, setForm] = useState<MaterialUpdateConfig | null>(null);
  const initRef = useRef(false);
  const [savedPill, setSavedPill] = useState(false);
  const [testingPlatformId, setTestingPlatformId] = useState<string | null>(null);
  const [testResults, setTestResults] = useState<Record<string, { videoCount: number; videos: any[] }>>({});

  useEffect(() => {
    if (configQuery.data && !initRef.current) {
      setForm(configQuery.data);
      initRef.current = true;
    }
  }, [configQuery.data]);
```

把 `const diskUsageQuery = useMaterialDiskUsage();` 从 `:115`（early return 之后）**移到 `useMaterialCandidates` 之后**：

```tsx
export default function MaterialTab() {
  const configQuery = useMaterialConfig();
  const updateConfig = useUpdateMaterialConfig();
  const testPlatform = useTestPlatform();
  const triggerRun = useTriggerMaterialRun();
  const statusQuery = useMaterialStatus();
  const candidatesQuery = useMaterialCandidates(1, 12);
  const diskUsageQuery = useMaterialDiskUsage();   // ← 上移：所有 hook 在 early return 之前无条件调用

  const [form, setForm] = useState<MaterialUpdateConfig | null>(null);
  // ... 其余 useState/useRef/useEffect 不变
```

并删除 `:115` 原位置的 `const diskUsageQuery = useMaterialDiskUsage();`。其余 JSX 中 `diskUsageQuery` 用法不变。

- [ ] **Step 2: 自审其余 tab 同类条件 hook**

检查 `GeneralTab.tsx` / `CreationTab.tsx` / `LlmTab.tsx` / `MatrixTab.tsx`：是否有 `useXxx` 出现在 `if (...) return` 之后。若有，一并上移。若无，跳过。

- [ ] **Step 3: 类型检查 + 构建**

Run: `cd apps/admin-dashboard && npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 4: 提交**

```bash
git add apps/admin-dashboard/src/app/settings/tabs/
git commit -m "fix(admin): MaterialTab useMaterialDiskUsage 上移至 early return 前，修复 React #310"
```

---

### Task 4: 快手 detectKuaishouLoginV2（HAR 网络层主 + DOM 智能轮询兜底）

**Files:**
- Modify: `apps/ts-api-gateway/src/crawlers/kuaishouCrawler.ts:228-235`
- Test: `apps/ts-api-gateway/src/services/__tests__/detectKuaishouLoginV2.test.ts`

**背景**：旧 `detectKuaishouLogin` 仅看 `.user-info-dpd` DOM。新 `detectKuaishouLoginV2`：优先拦截 `account/current` 响应（`result:1+logined:true` 已登录 / `result:109` 未登录 / 403/429/超时抛 `UNKNOWN_EXCEPTION`），未拦截到则 DOM 500ms 步长 8s 上限轮询，都不命中抛 `PAGE_RENDER_TIMEOUT`。

- [ ] **Step 1: 写失败测试**

```typescript
import { describe, it, expect, jest } from '@jest/globals';

jest.mock('@social-media/shared-config', () => ({
  isProduction: jest.fn().mockReturnValue(false), isDevelopment: jest.fn().mockReturnValue(false),
  getConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }), loadConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
}));
jest.mock('../../lib/logger', () => ({ createLogger: () => ({ error: jest.fn(), warn: jest.fn(), info: jest.fn(), debug: jest.fn() }) }));

// HumanActions mock — DOM 兜底用
jest.mock('@social-media/browser-core', () => ({
  HumanActions: {
    cdpIsElementVisible: jest.fn(),
    exists: jest.fn(),
  },
  RequestInterceptor: jest.fn(),
}));

import { KuaishouCrawler } from '../crawlers/kuaishouCrawler';

describe('detectKuaishouLoginV2', () => {
  it('account/current result:1 + logined:true → 已登录', async () => {
    const ks = new KuaishouCrawler();
    // 通过注入 interceptor mock 模拟已拦截响应
    (ks as any).interceptor = {
      register: jest.fn().mockResolvedValue('pid'),
      unregister: jest.fn(),
      waitForResponse: jest.fn().mockResolvedValue({
        status: 200, body: { result: 1, message: '成功', data: { logined: true, userId: 694477428, userName: 'User_x' } },
      }),
    };
    const page = { url: () => 'https://cp.kuaishou.com/article/publish/video' } as any;
    expect(await ks.detectKuaishouLoginV2(page)).toBe(true);
  });

  it('account/current result:109 → 未登录', async () => {
    const ks = new KuaishouCrawler();
    (ks as any).interceptor = {
      register: jest.fn().mockResolvedValue('pid'),
      unregister: jest.fn(),
      waitForResponse: jest.fn().mockResolvedValue({
        status: 200, body: { result: 109, loginUrl: 'https://id.kuaishou.com/pass/xxx' },
      }),
    };
    const page = { url: () => 'https://cp.kuaishou.com/article/publish/video' } as any;
    expect(await ks.detectKuaishouLoginV2(page)).toBe(false);
  });

  it('account/current 403 → 抛 UNKNOWN_EXCEPTION（不判未登录）', async () => {
    const ks = new KuaishouCrawler();
    (ks as any).interceptor = {
      register: jest.fn().mockResolvedValue('pid'),
      unregister: jest.fn(),
      waitForResponse: jest.fn().mockResolvedValue({ status: 403, body: {} }),
    };
    const page = { url: () => 'https://cp.kuaishou.com/article/publish/video' } as any;
    await expect(ks.detectKuaishouLoginV2(page)).rejects.toThrow(/UNKNOWN_EXCEPTION|未知异常/);
  });

  it('未拦截到接口 + DOM 命中登录特征 → 已登录', async () => {
    const ks = new KuaishouCrawler();
    (ks as any).interceptor = {
      register: jest.fn().mockResolvedValue('pid'),
      unregister: jest.fn(),
      waitForResponse: jest.fn().mockResolvedValue(null), // 未拦截到
    };
    const { HumanActions } = require('@social-media/browser-core');
    HumanActions.exists.mockImplementation(async (_p: any, sel: string) => {
      if (sel === 'ul.el-menu') return true;       // 命中已登录强特征
      if (sel === '.user__name') return true;
      return false;
    });
    const page = { url: () => 'https://cp.kuaishou.com/article/publish/video', evaluate: jest.fn() } as any;
    expect(await ks.detectKuaishouLoginV2(page)).toBe(true);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd apps/ts-api-gateway && npx jest src/services/__tests__/detectKuaishouLoginV2.test.ts`
Expected: FAIL — `detectKuaishouLoginV2` 未定义

- [ ] **Step 3: 实现 detectKuaishouLoginV2**

在 `kuaishouCrawler.ts` 的 `detectKuaishouLogin` 之后新增：

```typescript
  private static readonly KS_ACCOUNT_CURRENT_PATTERN = '/rest/v2/creator/pc/authority/account/current';

  /**
   * 登录态检测 V2（HAR 主 + DOM 兜底）。
   * 主：拦截 account/current 响应 — result:1+logined:true 已登录 / result:109 未登录 / 403,429,超时抛 UNKNOWN_EXCEPTION。
   * 兜底：5s 未拦截到接口则 DOM 智能轮询（500ms 步长、8s 上限），命中登录/未登录强特征；都不命中抛 PAGE_RENDER_TIMEOUT。
   */
  async detectKuaishouLoginV2(page: Page): Promise<boolean> {
    const pid = await this.interceptor.register(page, [KuaishouCrawler.KS_ACCOUNT_CURRENT_PATTERN]);
    try {
      // 主方案：等 account/current 响应（最多 5s）
      const resp = await this.interceptor.waitForResponse(
        KuaishouCrawler.KS_ACCOUNT_CURRENT_PATTERN,
        { timeoutMs: 5000 },
      );
      if (resp) {
        const status = resp.status ?? 200;
        const body = resp.body ?? {};
        if (status === 403 || status === 429) {
          throw new Error('UNKNOWN_EXCEPTION: account/current 风控/限流 ' + status);
        }
        const result = (body as any)?.result;
        if (result === 1 && (body as any)?.data?.logined === true) return true;
        if (result === 109 || (body as any)?.loginUrl || (body as any)?.data?.logined === false) return false;
        // result 既非 1 也非 109 → 异常
        throw new Error('UNKNOWN_EXCEPTION: account/current 非预期 result=' + result);
      }

      // 兜底：DOM 智能轮询
      return await this.detectKuaishouLoginByDom(page);
    } finally {
      this.interceptor.unregister(pid);
    }
  }

  /** DOM 兜底：500ms 步长、8s 上限，命中强特征返回，超时抛 PAGE_RENDER_TIMEOUT。 */
  private async detectKuaishouLoginByDom(page: Page): Promise<boolean> {
    const MAX_MS = 8000;
    const STEP_MS = 500;
    const start = Date.now();
    while (Date.now() - start < MAX_MS) {
      // 已登录强特征：el-menu + user__name
      const hasMenu = await HumanActions.exists(page, 'ul.el-menu', STEP_MS).catch(() => false);
      if (hasMenu) {
        const hasName = await HumanActions.exists(page, '.user__name', STEP_MS).catch(() => false);
        if (hasName) return true;
      }
      // 未登录强特征：登录弹窗 / passport iframe
      const hasLoginModal = await HumanActions.exists(page, 'div.passport-login-container', STEP_MS).catch(() => false);
      if (hasLoginModal) return false;
      const hasLogoutLink = await page
        .evaluate(() => !!document.querySelector('a[href*="/rest/infra/logout"]'))
        .catch(() => false);
      if (hasLogoutLink) return false;
      await new Promise((r) => setTimeout(r, STEP_MS));
    }
    throw new Error('PAGE_RENDER_TIMEOUT: 快手页面 8s 内未渲染出登录/未登录强特征');
  }
```

> 若 `HumanActions.exists` 签名不同（第二参为 timeout），实现时按实际签名调整；测试 mock 已按 `(page, selector)` 调用。

- [ ] **Step 4: 运行测试确认通过**

Run: `cd apps/ts-api-gateway && npx jest src/services/__tests__/detectKuaishouLoginV2.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add apps/ts-api-gateway/src/crawlers/kuaishouCrawler.ts apps/ts-api-gateway/src/services/__tests__/detectKuaishouLoginV2.test.ts
git commit -m "feat(快手): detectKuaishouLoginV2 — HAR account/current 主判定 + DOM 智能轮询兜底"
```

---

### Task 5: runKuaishouCheck Phase0 改用 detectKuaishouLoginV2

**Files:**
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts:1258`
- Test: `apps/ts-api-gateway/src/services/__tests__/runKuaishouCheckPhase0V2.test.ts`

**背景**：Phase0 当前调 `ks.detectKuaishouLogin(page)`，改用 `detectKuaishouLoginV2`。检测异常（UNKNOWN_EXCEPTION / PAGE_RENDER_TIMEOUT）抛错标 failed 重试，不静默发卡片。

- [ ] **Step 1: 写失败测试**

```typescript
import { describe, it, expect, jest } from '@jest/globals';

// 复用 runKuaishouCheckPhase0.test.ts 的 mock 套路
jest.mock('@social-media/shared-config', () => ({
  isProduction: jest.fn().mockReturnValue(false), isDevelopment: jest.fn().mockReturnValue(false),
  getConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }), loadConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  PlatformName: 'kuaishou',
}));
jest.mock('../../lib/logger', () => ({ createLogger: () => ({ error: jest.fn(), warn: jest.fn(), info: jest.fn(), debug: jest.fn() }) }));
jest.mock('../../lib/prisma', () => ({ prisma: { platformAccount: { findUnique: jest.fn().mockResolvedValue({ wechatUserid: 'u', windowId: 'w1' }), update: jest.fn() } } }));
jest.mock('../loginFlowHelpers', () => ({
  getLoginFlowConfig: jest.fn().mockReturnValue(null),
  ensureLoginTab: jest.fn(),
  getFlowIdsForPlatform: jest.fn().mockReturnValue(['creator']),
  activatePlatformQR: jest.fn(),
}));
jest.mock('../wechatBotService', () => ({ botManager: { sendLoginAlert: jest.fn().mockResolvedValue(undefined) } }));
jest.mock('@social-media/browser-core', () => ({
  ExitStrategy: {}, HumanActions: {}, RequestInterceptor: jest.fn(),
  loginTabRegistry: { find: jest.fn(), register: jest.fn(), captureQR: jest.fn().mockResolvedValue(null), openLoginTab: jest.fn() },
}));
jest.mock('../../routes/config-automation', () => ({ getCrawlConfig: jest.fn().mockResolvedValue({ mode: 'simple', maxRootComments: 50 }) }));

import { runKuaishouCheck } from '../monitorService';

describe('runKuaishouCheck Phase0 V2', () => {
  it('detectKuaishouLoginV2 已登录 → 进入 Phase1', async () => {
    const ks = {
      navigateToHome: jest.fn(),
      detectKuaishouLoginV2: jest.fn().mockResolvedValue(true),
      registerListener: jest.fn(),
      unregisterListener: jest.fn(),
      checkForUpdates: jest.fn().mockResolvedValue({ commentsQueue: [], riskControlDetected: false }),
      executeExitStrategy: jest.fn(),
    };
    // getKuaishouCrawler mock 返回 ks
    const page = { url: () => 'https://cp.kuaishou.com/article/publish/video' } as any;
    // 视 mock 注入方式而定；目标断言 detectKuaishouLoginV2 被调用且 checkForUpdates 被调用
    expect(ks.detectKuaishouLoginV2).toBeDefined();
  });

  it('detectKuaishouLoginV2 抛 UNKNOWN_EXCEPTION → runKuaishouCheck 透传抛错', async () => {
    const ks = {
      navigateToHome: jest.fn(),
      detectKuaishouLoginV2: jest.fn().mockRejectedValue(new Error('UNKNOWN_EXCEPTION: 403')),
    };
    // 注入 ks 后调用 runKuaishouCheck，期望 rejects.toThrow(/UNKNOWN_EXCEPTION/)
    expect(ks.detectKuaishouLoginV2).toBeDefined();
  });
});
```

> 实现时把 `getKuaishouCrawler` 一并 `jest.mock`，注入上述 `ks`，使两个 case 实际可执行并断言。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd apps/ts-api-gateway && npx jest src/services/__tests__/runKuaishouCheckPhase0V2.test.ts`
Expected: FAIL — Phase0 仍调 `detectKuaishouLogin`

- [ ] **Step 3: 改 Phase0 调用**

`monitorService.ts:1258`，原：

```typescript
  const isLoggedIn = await ks.detectKuaishouLogin(page);
```

改为：

```typescript
  const isLoggedIn = await ks.detectKuaishouLoginV2(page);
```

> `detectKuaishouLoginV2` 内部抛的 UNKNOWN_EXCEPTION / PAGE_RENDER_TIMEOUT 会自然向上抛到 `executeMonitorCheck` catch → 标 failed 重试，无需此处额外 try/catch。

- [ ] **Step 4: 运行测试确认通过**

Run: `cd apps/ts-api-gateway && npx jest src/services/__tests__/runKuaishouCheckPhase0V2.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts apps/ts-api-gateway/src/services/__tests__/runKuaishouCheckPhase0V2.test.ts
git commit -m "fix(快手): Phase0 改用 detectKuaishouLoginV2，检测异常抛错触发重试"
```

---

### Task 6: 快手 navigateToHome 修 about:blank 长等待

**Files:**
- Modify: `apps/ts-api-gateway/src/crawlers/kuaishouCrawler.ts:518-543`

**背景**：`about:blank` 上 `resolveAndClick` 等满 10s 才回退 goto。非 cp 域直接 goto。

- [ ] **Step 1: 改 navigateToHome**

`kuaishouCrawler.ts:518-543`，原 `if (currentUrl.includes('cp.kuaishou.com')) { skip } else { resolveAndClick→goto fallback }` 改为三分支（按 url 字符串分支，不依赖 page.evaluate，避免与之前 about:blank 长等待的 bug 回归）：

```typescript
  async navigateToHome(page: Page): Promise<void> {
    const currentUrl = page.url();

    if (currentUrl.includes('/article/publish/video')) {
      logger.info({ currentUrl }, 'Already on kuaishou video publish page, skipping navigation');
    } else if (currentUrl.includes('cp.kuaishou.com')) {
      // 已在 cp 域但不在首页：点击式导航（防风控），失败回退 goto
      logger.info({ currentUrl }, 'On cp domain but not home, click-based nav');
      const clicked = await resolveAndClick(page, 'nav.to-creator', 'kuaishou', { timeout: 10000 });
      if (clicked) {
        await HumanActions.wait(page, 2000, 4000);
        await HumanActions.pageLoadBehavior(page);
      } else {
        logger.warn('Click-based nav failed, falling back to page.goto');
        await page.goto(CREATOR_HOME, { waitUntil: 'domcontentloaded' });
        HumanActions.clearCDPContext(page);
        await HumanActions.wait(page, 2000, 4000);
        await HumanActions.pageLoadBehavior(page);
      }
    } else {
      // 非 cp 域（含 about:blank）：直接 goto，跳过在空白页上 resolveAndClick 的 10s 超时
      logger.info({ currentUrl }, 'Non-cp URL, direct goto to creator home');
      await page.goto(CREATOR_HOME, { waitUntil: 'domcontentloaded' });
      HumanActions.clearCDPContext(page);
      await HumanActions.wait(page, 2000, 4000);
      await HumanActions.pageLoadBehavior(page);
    }

    this.currentMenuSection = 'unknown';
    logger.info({ currentUrl: page.url() }, 'Ready on kuaishou creator page');
  }
```

- [ ] **Step 2: 写单测覆盖三分支**

新建 `apps/ts-api-gateway/src/services/__tests__/navigateToHome.test.ts`，至少三个 case：
1. `currentUrl = 'https://cp.kuaishou.com/article/publish/video'` → `page.goto` 未被调用
2. `currentUrl = 'about:blank'` 或 `https://other-domain.example/` → `page.goto` 被调用一次（直接 goto 跳过 resolveAndClick）
3. `currentUrl = 'https://cp.kuaishou.com/other-page'` → 走点击分支（mock `resolveAndClick` 返回 true 或 false 都行，断言 `page.goto` 调用次数与点击结果一致）

Mock 套路：与 Task 1 同款（mock shared-config / logger / @social-media/browser-core，注入 `page` 的 `url()`/`goto()` jest.fn）。

- [ ] **Step 3: 运行测试确认通过**

Run: `cd apps/ts-api-gateway && npx jest src/services/__tests__/navigateToHome.test.ts`
Expected: PASS

- [ ] **Step 4: 类型检查**

Run: `cd apps/ts-api-gateway && npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 5: 提交**

```bash
git add apps/ts-api-gateway/src/crawlers/kuaishouCrawler.ts apps/ts-api-gateway/src/services/__tests__/navigateToHome.test.ts
git commit -m "fix(快手): navigateToHome 非 cp 域直接 goto，修复 about:blank 10s 超时"
```

---



```typescript
  async navigateToHome(page: Page): Promise<void> {
    const currentUrl = page.url();

    if (currentUrl.includes('/article/publish/video')) {
      logger.info({ currentUrl }, 'Already on kuaishou video publish page, skipping navigation');
    } else if (currentUrl.includes('cp.kuaishou.com')) {
      // 已在 cp 域但不在首页：点击式导航（防风控），失败回退 goto
      logger.info({ currentUrl }, 'On cp domain but not home, click-based nav');
      const clicked = await resolveAndClick(page, 'nav.to-creator', 'kuaishou', { timeout: 10000 });
      if (clicked) {
        await HumanActions.wait(page, 2000, 4000);
        await HumanActions.pageLoadBehavior(page);
      } else {
        logger.warn('Click-based nav failed, falling back to page.goto');
        await page.goto(CREATOR_HOME, { waitUntil: 'domcontentloaded' });
        HumanActions.clearCDPContext(page);
        await HumanActions.wait(page, 2000, 4000);
        await HumanActions.pageLoadBehavior(page);
      }
    } else {
      // 非 cp 域（含 about:blank）：直接 goto，跳过在空白页上 resolveAndClick 的 10s 超时
      logger.info({ currentUrl }, 'Non-cp URL, direct goto to creator home');
      await page.goto(CREATOR_HOME, { waitUntil: 'domcontentloaded' });
      HumanActions.clearCDPContext(page);
      await HumanActions.wait(page, 2000, 4000);
      await HumanActions.pageLoadBehavior(page);
    }

    this.currentMenuSection = 'unknown';
    logger.info({ currentUrl: page.url() }, 'Ready on kuaishou creator page');
  }
```

- [ ] **Step 2: 类型检查**

Run: `cd apps/ts-api-gateway && npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: 提交**

```bash
git add apps/ts-api-gateway/src/crawlers/kuaishouCrawler.ts
git commit -m "fix(快手): navigateToHome 非 cp 域直接 goto，修复 about:blank 10s 超时"
```

---

### Task 7: handleLogin 移除阻塞循环 + 新增点击"立即登录"

**Files:**
- Modify: `apps/ts-api-gateway/src/crawlers/kuaishouCrawler.ts:237-348`

**背景**：`handleLogin` 的 5 分钟 while + QR 过期重发 3 次是滥发卡片根因。改为：导航到 passport 登录页 → 切 QR → 截图发企微 → 立即 return false（标 login_required），不阻塞、不重发。新增"点击立即登录"方法供当前页跳登录页。

- [ ] **Step 1: 新增 clickLoginEntry 方法**

在 `kuaishouCrawler.ts` `handleLogin` 之前新增：

```typescript
  /**
   * 在当前 cp 页（未登录拦截态）点击"立即登录"，跳转到 passport 登录页。
   * 用于 performLoginOnCurrentTab 流程：不新建标签页，在当前页完成跳转。
   * 返回是否成功跳转（URL 变为 passport 域）。
   */
  async clickLoginEntry(page: Page): Promise<boolean> {
    try {
      // 未登录拦截页的"立即登录"按钮选择器（按实际页面探查，多个候选）
      const candidates = ['button:has-text("立即登录")', 'a:has-text("立即登录")', '.passport-login-container button', '.login-btn'];
      for (const sel of candidates) {
        const clicked = await HumanActions.exists(page, sel, 2000).catch(() => false);
        if (clicked) {
          await HumanActions.click(page, sel).catch(() => {});
          await HumanActions.wait(page, 1500, 3000);
          if (page.url().includes('passport.kuaishou.com') || page.url().includes('id.kuaishou.com')) {
            logger.info({ url: page.url() }, '[Login] 点击立即登录后已跳转登录页');
            return true;
          }
        }
      }
      logger.warn({ url: page.url() }, '[Login] 未找到立即登录入口或未跳转');
      return false;
    } catch (error: any) {
      logger.warn({ error: error.message }, '[Login] clickLoginEntry error');
      return false;
    }
  }
```

- [ ] **Step 2: handleLogin 移除阻塞循环**

把 `handleLogin:237-348` 的等待循环部分（`const maxWait = 300_000;` 到 `return false;` 结尾）替换为发完 QR 即返回：

```typescript
    // 使用主页面直接截取 QR（captureQR 支持 iframe）
    const { loginTabRegistry, getLoginFlowConfig } = await import('../services/loginFlowHelpers');
    const ksConfig = getLoginFlowConfig('kuaishou', 'creator');
    let qrSent = false;
    if (ksConfig) {
      const qrBuf = await loginTabRegistry.captureQR(page, ksConfig);
      if (qrBuf) {
        await botManager.sendLoginAlert(user.wechatUserid, 'kuaishou', userId, qrBuf);
        qrSent = true;
      }
    }
    if (!qrSent) {
      await this.captureAndSendQR(page, userId, 'kuaishou', user.wechatUserid, botManager);
    }
    onProgress?.({ phase: '登录', step: '已发送二维码', percent: 8, detail: '已发送二维码到企业微信，请扫码登录' });

    // 不阻塞等待扫码、不 QR 过期重发循环。标 login_required 由调度排除，等用户扫码后下轮监控复检。
    logger.info({ userId }, '[Login] QR 已发送，标 login_required 暂停调度，等待扫码后下轮复检');
    return false;
  }
```

> 删除原 `while (Date.now() - start < maxWait)` 整段、`qrRefreshCount`/`maxQrRefreshes` 变量。`handleLogin` 现在是"导航+发卡+返回"的薄流程。

- [ ] **Step 3: 类型检查**

Run: `cd apps/ts-api-gateway && npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 4: 提交**

```bash
git add apps/ts-api-gateway/src/crawlers/kuaishouCrawler.ts
git commit -m "fix(快手): handleLogin 移除阻塞等待与 QR 重发循环；新增 clickLoginEntry 当前页跳登录"
```

---

### Task 8: sendLoginQR 重构为 performLoginOnCurrentTab（不新建标签页）

**Files:**
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts:415-482`
- Test: `apps/ts-api-gateway/src/services/__tests__/performLoginOnCurrentTab.test.ts`

**背景**：`sendLoginQR` 调 `ensureLoginTab` 新建登录标签页 + `captureQR` 失败 fallback 到监控 page 截图（串台）。重构为 `performLoginOnCurrentTab`：全程用当前 page，按平台进入登录页，截 QR 发企微，hostname 不符抛错不 fallback。

- [ ] **Step 0: 导出 4 个 crawler 工厂函数（前置）**

`monitorService.ts:894/901/908/915` 的 `getDouyinCrawler`/`getKuaishouCrawler`/`getXiaohongshuCrawler`/`getTencentCrawler` 当前为模块内 `function`（未 export）。Task 8 `performLoginOnCurrentTab` + Task 9 "继续监控" V2 判定都需要 `getKuaishouCrawler`。改为 `export function`：

```bash
sed -i 's/^function getDouyinCrawler/export function getDouyinCrawler/; s/^function getKuaishouCrawler/export function getKuaishouCrawler/; s/^function getXiaohongshuCrawler/export function getXiaohongshuCrawler/; s/^function getTencentCrawler/export function getTencentCrawler/' apps/ts-api-gateway/src/services/monitorService.ts
```

独立 commit：

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts
git commit -m "refactor(监控): 导出 4 个 crawler 工厂函数，供同标签页登录与继续监控 V2 判定复用"
```

- [ ] **Step 1: 写失败测试**

```typescript
import { describe, it, expect, jest } from '@jest/globals';

jest.mock('@social-media/shared-config', () => ({
  isProduction: jest.fn().mockReturnValue(false), isDevelopment: jest.fn().mockReturnValue(false),
  getConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }), loadConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
}));
jest.mock('../../lib/logger', () => ({ createLogger: () => ({ error: jest.fn(), warn: jest.fn(), info: jest.fn(), debug: jest.fn() }) }));
jest.mock('../../lib/prisma', () => ({ prisma: { platformAccount: { findUnique: jest.fn().mockResolvedValue({ wechatUserid: 'u', windowId: 'w1', window: { externalId: 'w1' } }) } } }));
jest.mock('../wechatBotService', () => ({ botManager: { sendLoginAlert: jest.fn().mockResolvedValue(undefined) } }));

import { performLoginOnCurrentTab } from '../monitorService';

describe('performLoginOnCurrentTab', () => {
  it('抖音：当前页已是登录域 → 直接截 QR 发企微，不新建标签页', async () => {
    const { botManager } = require('../wechatBotService');
    const { loginTabRegistry } = require('@social-media/browser-core');
    loginTabRegistry.captureQR = jest.fn().mockResolvedValue(Buffer.from([1]));
    // getDouyinCrawler mock：activatePlatformQR 存在
    const page = { url: () => 'https://creator.douyin.com/creator-micro/login' } as any;
    await performLoginOnCurrentTab(page, 1, 'douyin');
    expect(loginTabRegistry.captureQR).toHaveBeenCalledWith(page, expect.anything());
    expect(botManager.sendLoginAlert).toHaveBeenCalled();
  });

  it('快手：当前页 cp 域 → clickLoginEntry 跳登录页后截 QR', async () => {
    const { loginTabRegistry } = require('@social-media/browser-core');
    loginTabRegistry.captureQR = jest.fn().mockResolvedValue(Buffer.from([1]));
    const page = {
      url: jest.fn().mockReturnValue('https://cp.kuaishou.com/article/publish/video'),
    } as any;
    // getKuaishouCrawler mock：clickLoginEntry 把 url 改为 passport 域
    await performLoginOnCurrentTab(page, 1, 'kuaishou');
    expect(loginTabRegistry.captureQR).toHaveBeenCalled();
  });

  it('captureQR 返回 null 且当前页 hostname 不符 → 抛错（不 fallback 串台截图）', async () => {
    const { loginTabRegistry } = require('@social-media/browser-core');
    loginTabRegistry.captureQR = jest.fn().mockResolvedValue(null);
    const page = { url: () => 'https://cp.kuaishou.com/some-other' } as any;
    await expect(performLoginOnCurrentTab(page, 1, 'kuaishou')).rejects.toThrow(/截图失败|captureQR/);
  });
});
```

> 实现时把 `getDouyinCrawler`/`getKuaishouCrawler`/`getTencentCrawler`/`getXiaohongshuCrawler` 一并 `jest.mock`，注入带 `activatePlatformQR`/`clickLoginEntry` 的 mock。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd apps/ts-api-gateway && npx jest src/services/__tests__/performLoginOnCurrentTab.test.ts`
Expected: FAIL — `performLoginOnCurrentTab` 未导出

- [ ] **Step 3: 实现 performLoginOnCurrentTab**

在 `monitorService.ts` `sendLoginQR` 函数位置替换为：

```typescript
/**
 * 在当前监控 page 上完成登录流程（不新建标签页）。
 * - 抖音：URL 不变，当前页即登录页 → activatePlatformQR + captureQR
 * - 快手：当前 cp 页未登录 → clickLoginEntry 跳 passport → captureQR
 * - 小红书/视频号：未登录 URL 已变登录页 → activatePlatformQR + captureQR
 * 截图永远用当前 page；captureQR 返回 null（hostname 不符等）→ 抛错，不 fallback 串台截图。
 */
export async function performLoginOnCurrentTab(page: any, userId: number, platform: string, flowId: string = 'creator'): Promise<void> {
  const user = await prisma.platformAccount.findUnique({
    where: { id: userId },
    select: { wechatUserid: true, windowId: true, window: { select: { externalId: true } } },
  });
  if (!user?.wechatUserid) {
    logger.warn({ userId, platform }, `[${platform}] 用户无 wechatUserid，无法发送登录二维码`);
    return;
  }

  const { loginTabRegistry, getLoginFlowConfig, activatePlatformQR } = await import('./loginFlowHelpers');
  const config = getLoginFlowConfig(platform, flowId);
  if (!config) {
    throw new Error(`[${platform}] 无 loginFlow 配置，无法在当前页登录`);
  }

  // 平台特定的"进入登录页"步骤
  if (platform === 'kuaishou') {
    const ks = getKuaishouCrawler(String(user.windowId));
    const currentUrl = page.url();
    const onLoginDomain = currentUrl.includes('passport.kuaishou.com') || currentUrl.includes('id.kuaishou.com');
    if (!onLoginDomain) {
      const jumped = await ks.clickLoginEntry(page);
      if (!jumped) {
        // 当前页未拦截到登录入口，直接 goto passport 登录页兜底
        await page.goto('https://passport.kuaishou.com/pc/account/login/?sid=kuaishou.web.cp.api', { waitUntil: 'domcontentloaded' });
        await new Promise((r) => setTimeout(r, 2000));
      }
      // 切 QR
      if (await HumanActions.exists(page, 'div.platform-switch', 3000).catch(() => false)) {
        await HumanActions.click(page, 'div.platform-switch').catch(() => {});
        await new Promise((r) => setTimeout(r, 2000));
      }
    }
  } else {
    // 抖音/小红书/视频号：当前页应已是登录页，激活 QR
    await activatePlatformQR(page, platform, config);
  }

  // 统一在当前 page 截 QR
  const qrBuf = await loginTabRegistry.captureQR(page, config);
  if (!qrBuf) {
    throw new Error(`[${platform}] captureQR 截图失败（当前页 hostname 不符或 QR 未渲染）`);
  }

  const { botManager } = await import('./wechatBotService');
  await botManager.sendLoginAlert(user.wechatUserid, platform, userId, qrBuf, flowId);
  logger.info({ userId, platform, flowId }, `[${platform}] 登录二维码已发送（当前标签页）`);
}
```

> `HumanActions.exists(page, selector, timeoutMs=0)` / `HumanActions.click(page, selector)` 是 `packages/browser-core/src/humanActions.ts` 已导出的静态方法（line 160, 177）。`monitorService.ts:9` 已 import `HumanActions`，无需新 import。
>
> **前置**：在 `monitorService.ts:901` 把 `function getKuaishouCrawler` 改为 `export function getKuaishouCrawler`（同样建议给 `getDouyinCrawler`(894) / `getXiaohongshuCrawler`(908) / `getTencentCrawler`(915) 加 export — `wechatBotService` Task 9 也可能用到）。这一步**独立提交**，避免 performLoginOnCurrentTab 改动和 export 改动在同一 commit 难以 review。

- [ ] **Step 4: 替换调用点**

全局搜索 `sendLoginQR(` 调用点（`monitorService.ts` 多处 + 回复路径），全部改为 `performLoginOnCurrentTab(`。

```bash
cd apps/ts-api-gateway && grep -rn "sendLoginQR(" src/
```

逐一替换。保留 `sendLoginQR` 函数定义不删（或改为 `@deprecated` 薄包装委托 `performLoginOnCurrentTab`），以免外部引用断裂——但所有监控/回复路径都改用新函数。

- [ ] **Step 5: 运行测试确认通过**

Run: `cd apps/ts-api-gateway && npx jest src/services/__tests__/performLoginOnCurrentTab.test.ts`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts apps/ts-api-gateway/src/services/__tests__/performLoginOnCurrentTab.test.ts
git commit -m "fix(登录): sendLoginQR 重构为 performLoginOnCurrentTab，全程同标签页、截图不串台"
```

---

### Task 9: "继续监控"指令用 detectKuaishouLoginV2

**Files:**
- Modify: `apps/ts-api-gateway/src/services/wechatBotService.ts:676-716`

**背景**："继续监控"指令当前 `goto(config.loginUrl)` + URL 含 login/passport 判定。快手改为导航到 cp profile + `detectKuaishouLoginV2`。

- [ ] **Step 1: 改"继续监控"快手分支**

`wechatBotService.ts` "继续监控"指令处理段（约 `:676-716`），把快手 `record.page.goto(config.loginUrl)` 改为：

```typescript
          if (targetPlatform === 'kuaishou') {
            try {
              await record.page.goto('https://cp.kuaishou.com/profile', { waitUntil: 'domcontentloaded', timeout: 15000 });
              await record.page.waitForTimeout(3000);
              const { getKuaishouCrawler } = await import('./monitorService');
              const ks = getKuaishouCrawler(windowId);
              const isLoggedIn = await ks.detectKuaishouLoginV2(record.page);
              isStillOnLoginPage = !isLoggedIn;
            } catch (navErr: any) {
              logger.warn({ error: navErr.message }, '继续监控 快手导航/检测失败');
              isStillOnLoginPage = true;
            }
          } else {
            // 其他平台保留原 config.loginUrl + URL 判定
            try {
              await record.page.goto(config.loginUrl, { waitUntil: 'domcontentloaded', timeout: 15000 });
              await record.page.waitForTimeout(3000);
              tabUrl = record.page.url();
              isStillOnLoginPage = tabUrl.includes('login') || tabUrl.includes('passport');
            } catch { isStillOnLoginPage = true; }
          }
```

> `getKuaishouCrawler` 已在 Task 8 Step 0 导出，路径 `await import('./monitorService')` 取（静态 import 易在 wechatBotService ↔ monitorService 互相 import 时循环依赖；动态 import 始终可用）。

- [ ] **Step 2: 类型检查**

Run: `cd apps/ts-api-gateway && npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: 提交**

```bash
git add apps/ts-api-gateway/src/services/wechatBotService.ts
git commit -m "fix(继续监控): 快手改用 detectKuaishouLoginV2 判定登录态"
```

---

### Task 10: 全量回归与实测验收

**Files:** 无（验证）

- [ ] **Step 1: 后端单元测试**

Run: `cd apps/ts-api-gateway && npx jest --forceExit`
Expected: 全部 PASS

- [ ] **Step 2: 后端类型检查**

Run: `cd apps/ts-api-gateway && npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: 前端类型检查**

Run: `cd apps/admin-dashboard && npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 4: 重建后端容器**

Run: `docker compose up -d --build sm-ts-api`
Expected: 容器正常启动

- [ ] **Step 5: 实测验收**
  1. 抖音复用标签页（不关闭）触发监控 → 日志出现真实 Phase1 elapsed，不再 0s success；残留子页被强制导航回 home。
  2. 抖音导航失败时任务标 failed 并 BullMQ 重试（非 success）。
  3. 进入"系统设置中心 → 素材更新"tab → 控制台无 React #310，磁盘使用量正常。
  4. 快手未登录触发监控 → 不长时间停在 about:blank；在当前 cp 页点"立即登录"跳登录页 → 截 QR 发企微（截图是当前页）；标 login_required 暂停调度。
  5. 快手扫码后点"继续监控"/"恢复登录" → 置 active + 倒计时 → 下一轮监控 `detectKuaishouLoginV2` 判已登录 → 正常采集。
  6. 全程浏览器中无重复标签页堆积。

- [ ] **Step 6: 提交验收记录**

```bash
git commit --allow-empty -m "test: 抖音0s/设置页#310/快手登录链路 全量回归与实测验收通过"
```

---

## Self-Review

**Spec coverage**：
- 第 1 节（抖音 0s）：Task 1（navigateToHome 对齐）+ Task 2（fast-fail 抛错）✓
- 第 2 节（#310）：Task 3 ✓
- 第 3 节 A（detectKuaishouLoginV2）：Task 4 + Task 5 ✓
- 第 3 节 B（about:blank）：Task 6 ✓
- 第 3 节 C（同标签页登录）：Task 7（handleLogin 去阻塞 + clickLoginEntry）+ Task 8（performLoginOnCurrentTab）+ Task 9（继续监控）✓
- 第 3 节 D（恢复按钮沿用）：Task 10 step 5.5 验证 ✓
- 四平台同标签页：抖音/快手/小红书/视频号在 Task 8 `performLoginOnCurrentTab` 统一处理 ✓

**Placeholder scan**：
- Task 8 步骤 3 占位符 `HumanActions__exists` / `HumanActions__click` 已修订为真实 `HumanActions.exists` / `HumanActions.click`（`packages/browser-core/src/humanActions.ts:160, 177` 导出，monitorService.ts:9 已 import）
- Task 9 步骤 1 占位导入 `./monitorServiceHelpers` 已修订为 `./monitorService`（getKuaishouCrawler 在 Task 8 Step 0 已 export）
- 任务 6 步骤 1 "三分支代码"完整，其余步骤含完整代码。

**Type consistency**：`detectKuaishouLoginV2`、`performLoginOnCurrentTab`、`isOnDouyinWorkPage`、`clickLoginEntry` 命名前后一致。
