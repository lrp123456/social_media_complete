# 登录态检测与恢复按钮修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复快手登录态误判、抖音监控 0s 假成功、恢复登录按钮不生效、快手/视频号滥发登录卡片四个 bug。

**Architecture:** 监控轮询只检测不阻塞——失效发一张卡片+停调度；登录态检测改用平台页面右上角用户信息模块；恢复登录按钮改为直接置 active + 启动倒计时；抖音入口改按"工作页"判导航。视频号回复路径保留阻塞等待扫码语义。

**Tech Stack:** TypeScript, Patchright/Playwright, Prisma, BullMQ, React (admin-dashboard), Vitest, node --test (browser-core)

---

## 文件结构

| 文件 | 责任 |
|---|---|
| `apps/ts-api-gateway/src/crawlers/kuaishouCrawler.ts` | 新增 `detectKuaishouLogin`；`handleLogin` 改薄封装 |
| `apps/ts-api-gateway/src/crawlers/tencentCrawler.ts` | 新增 `detectTencentLogin`；`handleLogin` 监控用薄封装 + 回复用阻塞 |
| `apps/ts-api-gateway/src/services/monitorService.ts` | `runKuaishouCheck`/`runDouyinCheck`/`runTencentCheck` 入口改造；新增 `recoverLogin` 导出 |
| `apps/ts-api-gateway/src/services/wechatBotService.ts` | "继续监控"指令改用 profile 页 + `.user-info-dpd` 判断 |
| `apps/ts-api-gateway/src/routes/matrix.ts` | 新增 `POST /monitor/accounts/:userId/recover-login` |
| `apps/admin-dashboard/src/hooks/useApi.ts` | 新增 `useRecoverLogin` |
| `apps/admin-dashboard/src/app/matrix/page.tsx` | "恢复登录"按钮改调 `useRecoverLogin` |

测试：
- `apps/ts-api-gateway/src/services/__tests__/detectKuaishouLogin.test.ts`（新）
- `apps/ts-api-gateway/src/services/__tests__/detectTencentLogin.test.ts`（新）
- `apps/ts-api-gateway/src/services/__tests__/runDouyinCheckEntry.test.ts`（新）
- `apps/ts-api-gateway/src/services/__tests__/recoverLogin.test.ts`（新）
- `apps/ts-api-gateway/src/services/__tests__/markLoginRecovered.test.ts`（已存在，不改）

---

## Task 1: 快手 `detectKuaishouLogin` 检测函数

**Files:**
- Modify: `apps/ts-api-gateway/src/crawlers/kuaishouCrawler.ts:185-215`（`checkLoginStatus` 之后新增 `detectKuaishouLogin`）
- Test: `apps/ts-api-gateway/src/services/__tests__/detectKuaishouLogin.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `apps/ts-api-gateway/src/services/__tests__/detectKuaishouLogin.test.ts`：

```typescript
import { describe, it, expect, jest } from '@jest/globals';

jest.mock('@social-media/shared-config', () => ({
  isProduction: jest.fn().mockReturnValue(false),
  isDevelopment: jest.fn().mockReturnValue(false),
  getConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  loadConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  PlatformName: 'douyin',
}));

jest.mock('../../lib/logger', () => ({
  createLogger: () => ({ error: jest.fn(), warn: jest.fn(), info: jest.fn(), debug: jest.fn() }),
}));

jest.mock('@social-media/browser-core', () => ({
  HumanActions: {
    cdpIsElementVisible: jest.fn(),
    cdpGetBodyText: jest.fn(),
    cdpClick: jest.fn(),
    exists: jest.fn(),
    click: jest.fn(),
    wait: jest.fn(),
    pageLoadBehavior: jest.fn(),
    clearCDPContext: jest.fn(),
  },
}));

import { HumanActions } from '@social-media/browser-core';
import { KuaishouCrawler } from '../../crawlers/kuaishouCrawler';

describe('KuaishouCrawler.detectKuaishouLogin', () => {
  beforeEach(() => jest.clearAllMocks());

  it('右上角 .user-info-dpd 可见 → 已登录', async () => {
    (HumanActions.cdpIsElementVisible as jest.Mock).mockResolvedValue(true);
    const ks = new KuaishouCrawler();
    const page = { url: () => 'https://cp.kuaishou.com/article/publish/video' } as any;
    const result = await ks.detectKuaishouLogin(page);
    expect(result).toBe(true);
    expect(HumanActions.cdpIsElementVisible).toHaveBeenCalledWith(page, '.user-info-dpd');
  });

  it('.user-info-dpd 不存在 → 未登录', async () => {
    (HumanActions.cdpIsElementVisible as jest.Mock).mockResolvedValue(false);
    const ks = new KuaishouCrawler();
    const page = { url: () => 'https://cp.kuaishou.com/profile' } as any;
    const result = await ks.detectKuaishouLogin(page);
    expect(result).toBe(false);
  });

  it('在 passport 登录页（无 .user-info-dpd）→ 未登录，不因 URL 含 passport 直接判', async () => {
    (HumanActions.cdpIsElementVisible as jest.Mock).mockResolvedValue(false);
    const ks = new KuaishouCrawler();
    const page = { url: () => 'https://passport.kuaishou.com/pc/account/login/' } as any;
    const result = await ks.detectKuaishouLogin(page);
    expect(result).toBe(false);
    expect(HumanActions.cdpIsElementVisible).toHaveBeenCalledWith(page, '.user-info-dpd');
  });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/detectKuaishouLogin.test.ts`
Expected: FAIL — `detectKuaishouLogin is not a function`

- [ ] **Step 3: 实现 `detectKuaishouLogin`**

在 `apps/ts-api-gateway/src/crawlers/kuaishouCrawler.ts` 的 `checkLoginStatus` 方法之后（约 `:215` 闭括号后）新增：

```typescript
  /**
   * 登录态检测（不导航，不阻塞）。
   * 快手任意 cp 页面右上角均有 .user-info-dpd 用户状态栏；登录失效时该元素消失。
   * 替代 checkLoginStatus 的 URL/bodyText 误判逻辑（passport 域直接判 false、sidebar 选择器漏命中）。
   */
  async detectKuaishouLogin(page: Page): Promise<boolean> {
    try {
      return await HumanActions.cdpIsElementVisible(page, '.user-info-dpd');
    } catch (error: any) {
      logger.warn({ error: error.message }, '[Login] detectKuaishouLogin error');
      return false;
    }
  }
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/detectKuaishouLogin.test.ts`
Expected: PASS（3 tests passed）

- [ ] **Step 5: 提交**

```bash
git add apps/ts-api-gateway/src/crawlers/kuaishouCrawler.ts apps/ts-api-gateway/src/services/__tests__/detectKuaishouLogin.test.ts
git commit -m "feat(快手): 新增 detectKuaishouLogin 基于 .user-info-dpd 检测登录态"
```

---

## Task 2: 快手 `runKuaishouCheck` Phase 0 移除 handleLogin 阻塞

**Files:**
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts:1226-1241`
- Test: `apps/ts-api-gateway/src/services/__tests__/runKuaishouCheckPhase0.test.ts`（新）

- [ ] **Step 1: 写失败测试**

创建 `apps/ts-api-gateway/src/services/__tests__/runKuaishouCheckPhase0.test.ts`：

```typescript
import { describe, it, expect, jest } from '@jest/globals';

jest.mock('@social-media/shared-config', () => ({
  isProduction: jest.fn().mockReturnValue(false),
  isDevelopment: jest.fn().mockReturnValue(false),
  getConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  loadConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  PlatformName: 'douyin',
}));

jest.mock('../../lib/logger', () => ({
  createLogger: () => ({ error: jest.fn(), warn: jest.fn(), info: jest.fn(), debug: jest.fn() }),
}));

jest.mock('../../lib/prisma', () => ({ prisma: { platformAccount: { findUnique: jest.fn(), update: jest.fn() } } }));
jest.mock('../../lib/redis', () => ({ getRedis: () => ({ del: jest.fn(), get: jest.fn(), set: jest.fn() }) }));
jest.mock('../wechatBotService', () => ({ botManager: { sendLoginAlert: jest.fn() } }));
jest.mock('../loginFlowHelpers', () => ({
  getFlowIdsForPlatform: jest.fn().mockReturnValue(['creator']),
  getLoginFlowConfig: jest.fn(),
  loginTabRegistry: { find: jest.fn(), captureQR: jest.fn() },
}));

const mockDetectKuaishouLogin = jest.fn();
const mockUpdateUserStatus = jest.fn();
jest.mock('../monitorDatabaseService', () => ({
  getCrawlMode: jest.fn().mockResolvedValue('simple'),
  updateUserStatus: mockUpdateUserStatus,
  getCrawlConfig: jest.fn().mockResolvedValue({ mode: 'simple', maxRootComments: 50 }),
}));

// Mock KuaishouCrawler — detectKuaishouLogin 是新方法
jest.mock('../../crawlers/kuaishouCrawler', () => ({
  KuaishouCrawler: jest.fn().mockImplementation(() => ({
    detectKuaishouLogin: mockDetectKuaishouLogin,
    navigateToHome: jest.fn(),
    registerListener: jest.fn(),
    unregisterListener: jest.fn(),
    checkForUpdates: jest.fn(),
  })),
}));

import { runKuaishouCheck } from '../monitorService';

describe('runKuaishouCheck Phase0', () => {
  it('未登录 → 标 login_required + return，不阻塞', async () => {
    mockDetectKuaishouLogin.mockResolvedValue(false);
    const task = { userId: 11, windowId: 'w1', platform: 'kuaishou' } as any;
    const page = { url: () => 'https://cp.kuaishou.com/article/publish/video' } as any;
    const result = await runKuaishouCheck(page, task);
    expect(result.hasUpdate).toBe(false);
    expect(mockUpdateUserStatus).toHaveBeenCalledWith(11, 'login_required');
  });

  it('已登录 → 继续（不调 updateUserStatus）', async () => {
    mockDetectKuaishouLogin.mockResolvedValue(true);
    const task = { userId: 11, windowId: 'w1', platform: 'kuaishou' } as any;
    const page = { url: () => 'https://cp.kuaishou.com/article/publish/video' } as any;
    await runKuaishouCheck(page, task);
    expect(mockUpdateUserStatus).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/runKuaishouCheckPhase0.test.ts`
Expected: FAIL — 当前 Phase0 调 `handleLogin`（阻塞），未登录会卡住/超时，且不调 `detectKuaishouLogin`

- [ ] **Step 3: 改造 Phase 0**

修改 `apps/ts-api-gateway/src/services/monitorService.ts:1226-1241`，把：

```typescript
  // Phase 0: 登录检测
  onProgress?.({ phase: 'Phase0', step: '检测登录状态', percent: 5, detail: '正在检测快手登录状态' });

  // 先导航到快手创作者中心
  const currentUrl = page.url();
  if (!currentUrl.includes('cp.kuaishou.com')) {
    await ks.navigateToHome(page);
  }

  // 检测登录状态（支持扫码等待）
  const loginSuccess = await ks.handleLogin(page, task.userId, onProgress);
  if (!loginSuccess) {
    logger.error({ userId: task.userId }, '快手登录失败');
    await db.updateUserStatus(task.userId, 'login_required');
    return { hasUpdate: false, newComments: 0, updatedVideos: [], phase: 'Phase1', riskDetected: false };
  }
```

替换为：

```typescript
  // Phase 0: 登录检测（只检测不阻塞；失效发一张卡片+停调度，不跳登录页不等扫码）
  onProgress?.({ phase: 'Phase0', step: '检测登录状态', percent: 5, detail: '正在检测快手登录状态' });

  // 先导航到快手创作者中心
  const currentUrl = page.url();
  if (!currentUrl.includes('cp.kuaishou.com')) {
    await ks.navigateToHome(page);
  }

  const isLoggedIn = await ks.detectKuaishouLogin(page);
  if (!isLoggedIn) {
    logger.warn({ userId: task.userId }, '快手登录态失效，发卡片并停调度');
    await db.updateUserStatus(task.userId, 'login_required');
    await sendLoginQR(page, task.userId, 'kuaishou');
    return { hasUpdate: false, newComments: 0, updatedVideos: [], phase: 'Phase1', riskDetected: false };
  }
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/runKuaishouCheckPhase0.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts apps/ts-api-gateway/src/services/__tests__/runKuaishouCheckPhase0.test.ts
git commit -m "fix(快手): Phase0 改用 detectKuaishouLogin，移除 handleLogin 阻塞"
```

---

## Task 3: 抖音 `runDouyinCheck` 入口修复 0s 假成功

**Files:**
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts:1048-1059`
- Test: `apps/ts-api-gateway/src/services/__tests__/runDouyinCheckEntry.test.ts`（新）

- [ ] **Step 1: 写失败测试**

创建 `apps/ts-api-gateway/src/services/__tests__/runDouyinCheckEntry.test.ts`：

```typescript
import { describe, it, expect, jest } from '@jest/globals';

jest.mock('@social-media/shared-config', () => ({
  isProduction: jest.fn().mockReturnValue(false),
  isDevelopment: jest.fn().mockReturnValue(false),
  getConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  loadConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  PlatformName: 'douyin',
}));

jest.mock('../../lib/logger', () => ({
  createLogger: () => ({ error: jest.fn(), warn: jest.fn(), info: jest.fn(), debug: jest.fn() }),
}));

jest.mock('../../lib/prisma', () => ({ prisma: { platformAccount: { findUnique: jest.fn() } } }));
jest.mock('../../lib/redis', () => ({ getRedis: () => ({ del: jest.fn(), get: jest.fn(), set: jest.fn() }) }));
jest.mock('../wechatBotService', () => ({ botManager: { sendLoginAlert: jest.fn() } }));
jest.mock('../loginFlowHelpers', () => ({
  getFlowIdsForPlatform: jest.fn().mockReturnValue(['creator']),
  getLoginFlowConfig: jest.fn(),
  loginTabRegistry: { find: jest.fn(), captureQR: jest.fn() },
}));

jest.mock('../monitorDatabaseService', () => ({
  getCrawlMode: jest.fn().mockResolvedValue('simple'),
  getCrawlConfig: jest.fn().mockResolvedValue({ mode: 'simple', maxRootComments: 50 }),
  updateUserStatus: jest.fn(),
}));

const mockNavigateToCreatorHome = jest.fn();
const mockCheckForUpdates = jest.fn();
jest.mock('../../crawlers/douyinCrawler', () => ({
  DouyinCrawler: jest.fn().mockImplementation(() => ({
    navigateToCreatorHome: mockNavigateToCreatorHome,
    registerListener: jest.fn(),
    unregisterListener: jest.fn(),
    checkForUpdates: mockCheckForUpdates.mockResolvedValue({ commentsQueue: [], riskControlDetected: false }),
    executeExitStrategy: jest.fn(),
  })),
}));

import { runDouyinCheck } from '../monitorService';

describe('runDouyinCheck 入口导航', () => {
  it('残留子页（/data/following/follower）→ 应触发 navigateToCreatorHome', async () => {
    const task = { userId: 6, windowId: 'w1', platform: 'douyin' } as any;
    const page = {
      url: jest.fn().mockReturnValueOnce('https://creator.douyin.com/creator-micro/data/following/follower')
        .mockReturnValue('https://creator.douyin.com/creator-micro/home'),
    } as any;
    await runDouyinCheck(page, task);
    expect(mockNavigateToCreatorHome).toHaveBeenCalled();
  });

  it('已在 home 页 → 不触发 navigateToCreatorHome', async () => {
    const task = { userId: 6, windowId: 'w1', platform: 'douyin' } as any;
    const page = { url: jest.fn().mockReturnValue('https://creator.douyin.com/creator-micro/home') } as any;
    await runDouyinCheck(page, task);
    expect(mockNavigateToCreatorHome).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/runDouyinCheckEntry.test.ts`
Expected: FAIL — 当前按 `currentUrl.includes('creator.douyin.com')` 判跳过导航，残留子页不触发 `navigateToCreatorHome`

- [ ] **Step 3: 改造入口**

修改 `apps/ts-api-gateway/src/services/monitorService.ts:1048-1059`，把：

```typescript
  const currentUrl = page.url();
  if (!currentUrl.includes('creator.douyin.com')) {
    await dy.navigateToCreatorHome(page);
  }

  // wrong-page fast-fail：偏离 creator-micro 主页/内容管理 → fast-fail
  const dyUrl = page.url();
  const dyOk = dyUrl.includes('/creator-micro/home') || dyUrl.includes('/creator-micro/content/manage');
  if (!dyOk) {
    logger.warn({ userId: task.userId, url: dyUrl }, '[抖音] Phase1 入口 page 偏离作品管理页，fast-fail');
    return { hasUpdate: false, newComments: 0, updatedVideos: [], phase: 'Phase1', riskDetected: false };
  }
```

替换为：

```typescript
  // 入口按"是否在工作页"判导航（而非是否在 douyin 域），
  // 避免上一轮退出策略残留子页（/data/following/follower 等）直接 fast-fail 0s 假成功
  const entryUrl = page.url();
  const onWorkPage = entryUrl.includes('/creator-micro/home') || entryUrl.includes('/creator-micro/content/manage');
  if (!onWorkPage) {
    await dy.navigateToCreatorHome(page);
  }

  // 导航后再次校验，仍偏离才 fast-fail（兜底，非常态）
  const dyUrl = page.url();
  const dyOk = dyUrl.includes('/creator-micro/home') || dyUrl.includes('/creator-micro/content/manage');
  if (!dyOk) {
    logger.warn({ userId: task.userId, url: dyUrl }, '[抖音] Phase1 入口 page 偏离作品管理页，fast-fail');
    return { hasUpdate: false, newComments: 0, updatedVideos: [], phase: 'Phase1', riskDetected: false };
  }
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/runDouyinCheckEntry.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts apps/ts-api-gateway/src/services/__tests__/runDouyinCheckEntry.test.ts
git commit -m "fix(抖音): 入口按工作页判导航，修复残留子页 0s 假成功"
```

---

## Task 4: 视频号 `detectTencentLogin` + `handleLogin` 监控薄封装

**Files:**
- Modify: `apps/ts-api-gateway/src/crawlers/tencentCrawler.ts:149-180`
- Test: `apps/ts-api-gateway/src/services/__tests__/detectTencentLogin.test.ts`（新）

- [ ] **Step 1: 写失败测试**

创建 `apps/ts-api-gateway/src/services/__tests__/detectTencentLogin.test.ts`：

```typescript
import { describe, it, expect, jest } from '@jest/globals';

jest.mock('@social-media/shared-config', () => ({
  isProduction: jest.fn().mockReturnValue(false),
  isDevelopment: jest.fn().mockReturnValue(false),
  getConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  loadConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  PlatformName: 'douyin',
}));

jest.mock('../../lib/logger', () => ({
  createLogger: () => ({ error: jest.fn(), warn: jest.fn(), info: jest.fn(), debug: jest.fn() }),
}));

jest.mock('@social-media/browser-core', () => ({
  HumanActions: {
    cdpIsElementVisible: jest.fn(),
    cdpGetBodyText: jest.fn(),
    cdpClick: jest.fn(),
    wait: jest.fn(),
    pageLoadBehavior: jest.fn(),
    cdpClickByText: jest.fn(),
  },
}));

import { HumanActions } from '@social-media/browser-core';
import { TencentCrawler } from '../../crawlers/tencentCrawler';

describe('TencentCrawler.detectTencentLogin', () => {
  beforeEach(() => jest.clearAllMocks());

  it('在 /platform 页（非 /login）→ 已登录', async () => {
    (HumanActions.cdpIsElementVisible as jest.Mock).mockResolvedValue(true);
    const tc = new TencentCrawler();
    const page = { url: () => 'https://channels.weixin.qq.com/platform/home' } as any;
    const result = await tc.detectTencentLogin(page);
    expect(result).toBe(true);
  });

  it('在 /login 页 → 未登录', async () => {
    (HumanActions.cdpIsElementVisible as jest.Mock).mockResolvedValue(false);
    const tc = new TencentCrawler();
    const page = { url: () => 'https://channels.weixin.qq.com/login.html' } as any;
    const result = await tc.detectTencentLogin(page);
    expect(result).toBe(false);
  });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/detectTencentLogin.test.ts`
Expected: FAIL — `detectTencentLogin is not a function`

- [ ] **Step 3: 实现 `detectTencentLogin` + 重构 `handleLogin` 监控分支**

在 `apps/ts-api-gateway/src/crawlers/tencentCrawler.ts:149` 的 `handleLogin` 方法**之前**新增 `detectTencentLogin`，并把 `handleLogin` 的"在平台页 → 登录有效"分支改为复用它。修改 `:149-180`：

```typescript
  /**
   * 登录态检测（不导航，不阻塞）。
   * 在 /platform 页（非 /login）即视为已登录。供监控 Phase0 使用。
   */
  async detectTencentLogin(page: Page): Promise<boolean> {
    try {
      const url = page.url();
      return url.includes('/platform') && !url.includes('/login');
    } catch (error: any) {
      logger.warn({ error: error.message }, '[Login] detectTencentLogin error');
      return false;
    }
  }

  async handleLogin(page: Page, userId: number): Promise<boolean> {
    logger.info('[Login] Checking login status');

    const currentUrl = page.url();

    // 在登录页 → 需要扫码（回复路径需阻塞等待扫码）
    if (currentUrl.includes('/login')) {
      logger.info({ currentUrl }, '[Login] On login page, need QR scan');
      return await this.handleQRLogin(page, userId);
    }

    // 在平台页面 → 登录有效
    if (currentUrl.includes('/platform')) {
      logger.info({ currentUrl }, '[Login] On platform page, session valid');
      return true;
    }

    // 不在平台页也不在登录页 → 导航到首页检查
    logger.info({ currentUrl }, '[Login] Navigating to home to check status');
    await page.goto(TENCENT_HOME, { waitUntil: 'domcontentloaded' });
    await HumanActions.wait(page, 3000, 5000);

    const url = page.url();
    if (url.includes('/platform') && !url.includes('/login')) {
      logger.info('[Login] Session valid after navigation');
      return true;
    }

    // 被重定向到登录页
    logger.info('[Login] Redirected to login page, need QR scan');
    return await this.handleQRLogin(page, userId);
  }
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/detectTencentLogin.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add apps/ts-api-gateway/src/crawlers/tencentCrawler.ts apps/ts-api-gateway/src/services/__tests__/detectTencentLogin.test.ts
git commit -m "feat(视频号): 新增 detectTencentLogin 薄检测，handleLogin 保留阻塞供回复路径"
```

---

## Task 5: 视频号 `runTencentCheck` Phase 0 改用 detectTencentLogin

**Files:**
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts:1608-1615`
- Test: `apps/ts-api-gateway/src/services/__tests__/runTencentCheckPhase0.test.ts`（新）

- [ ] **Step 1: 写失败测试**

创建 `apps/ts-api-gateway/src/services/__tests__/runTencentCheckPhase0.test.ts`：

```typescript
import { describe, it, expect, jest } from '@jest/globals';

jest.mock('@social-media/shared-config', () => ({
  isProduction: jest.fn().mockReturnValue(false),
  isDevelopment: jest.fn().mockReturnValue(false),
  getConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  loadConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  PlatformName: 'douyin',
}));

jest.mock('../../lib/logger', () => ({
  createLogger: () => ({ error: jest.fn(), warn: jest.fn(), info: jest.fn(), debug: jest.fn() }),
}));
jest.mock('../../lib/prisma', () => ({ prisma: { platformAccount: { findUnique: jest.fn() } } }));
jest.mock('../../lib/redis', () => ({ getRedis: () => ({ del: jest.fn(), get: jest.fn(), set: jest.fn() }) }));
jest.mock('../wechatBotService', () => ({ botManager: { sendLoginAlert: jest.fn() } }));
jest.mock('../loginFlowHelpers', () => ({
  getFlowIdsForPlatform: jest.fn().mockReturnValue(['creator']),
  getLoginFlowConfig: jest.fn(),
  loginTabRegistry: { find: jest.fn(), captureQR: jest.fn() },
}));
jest.mock('../monitorDatabaseService', () => ({
  getCrawlMode: jest.fn().mockResolvedValue('simple'),
  getCrawlConfig: jest.fn().mockResolvedValue({ mode: 'simple', maxRootComments: 50 }),
  updateUserStatus: jest.fn(),
}));

const mockDetectTencentLogin = jest.fn();
jest.mock('../../crawlers/tencentCrawler', () => ({
  TencentCrawler: jest.fn().mockImplementation(() => ({
    detectTencentLogin: mockDetectTencentLogin,
    registerListener: jest.fn(),
    unregisterListener: jest.fn(),
    checkForUpdates: jest.fn(),
  })),
}));

import { runTencentCheck } from '../monitorService';

describe('runTencentCheck Phase0', () => {
  it('未登录 → 标 login_required + return，不阻塞 handleLogin', async () => {
    mockDetectTencentLogin.mockResolvedValue(false);
    const task = { userId: 11, windowId: 'w1', platform: 'tencent' } as any;
    const page = { url: () => 'https://channels.weixin.qq.com/login.html' } as any;
    const result = await runTencentCheck(page, task);
    expect(result.hasUpdate).toBe(false);
  });

  it('已登录 → 继续', async () => {
    mockDetectTencentLogin.mockResolvedValue(true);
    const task = { userId: 11, windowId: 'w1', platform: 'tencent' } as any;
    const page = { url: () => 'https://channels.weixin.qq.com/platform/home' } as any;
    await runTencentCheck(page, task);
    expect(mockDetectTencentLogin).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/runTencentCheckPhase0.test.ts`
Expected: FAIL — 当前调 `handleLogin`（阻塞 120s），未用 `detectTencentLogin`

- [ ] **Step 3: 改造 Phase 0**

修改 `apps/ts-api-gateway/src/services/monitorService.ts:1608-1615`，把：

```typescript
  // Phase 0: 登录检测
  onProgress?.({ phase: 'Phase0', step: '检测登录状态', percent: 10, detail: '正在检测视频号登录状态' });
  const loggedIn = await tc.handleLogin(page, task.userId);
  if (!loggedIn) {
    logger.error({ userId: task.userId }, '视频号登录失败');
    await db.updateUserStatus(task.userId, 'login_required');
    return { hasUpdate: false, newComments: 0, updatedVideos: [], phase: 'Phase1', riskDetected: false };
  }
```

替换为：

```typescript
  // Phase 0: 登录检测（只检测不阻塞；失效发一张卡片+停调度）
  onProgress?.({ phase: 'Phase0', step: '检测登录状态', percent: 10, detail: '正在检测视频号登录状态' });
  const loggedIn = await tc.detectTencentLogin(page);
  if (!loggedIn) {
    logger.warn({ userId: task.userId }, '视频号登录态失效，发卡片并停调度');
    await db.updateUserStatus(task.userId, 'login_required');
    await sendLoginQR(page, task.userId, 'tencent');
    return { hasUpdate: false, newComments: 0, updatedVideos: [], phase: 'Phase1', riskDetected: false };
  }
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/runTencentCheckPhase0.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts apps/ts-api-gateway/src/services/__tests__/runTencentCheckPhase0.test.ts
git commit -m "fix(视频号): Phase0 改用 detectTencentLogin，移除监控路径 handleLogin 阻塞"
```

---

## Task 6: "继续监控"指令改用 profile 页 + `.user-info-dpd`

**Files:**
- Modify: `apps/ts-api-gateway/src/services/wechatBotService.ts:705-716`

- [ ] **Step 1: 阅读当前实现确认行号**

Run: `sed -n '700,720p' apps/ts-api-gateway/src/services/wechatBotService.ts`
确认 `:705-716` 是 `isStillOnLoginPage` 判断 + `goto(config.loginUrl)` 块。

- [ ] **Step 2: 改造判断逻辑**

修改 `apps/ts-api-gateway/src/services/wechatBotService.ts:705-716`，把：

```typescript
          // 检查登录标签页是否已从登录页跳转（URL 不再包含 login/passport）
          let tabUrl = record.page.url();
          let isStillOnLoginPage = tabUrl.includes('login') || tabUrl.includes('passport');
          if (isStillOnLoginPage) {
            // 主动导航到平台首页，验证是否真正登录
            try {
              await record.page.goto(config.loginUrl, { waitUntil: 'domcontentloaded', timeout: 15000 });
              await record.page.waitForTimeout(3000);
              tabUrl = record.page.url();
              isStillOnLoginPage = tabUrl.includes('login') || tabUrl.includes('passport');
            } catch { /* navigation failed, assume still on login */ }
          }
```

替换为：

```typescript
          // 导航到快手 profile 页验证登录态（登录成功后右上角 .user-info-dpd 存在；失效会回退到登录页）
          // 其他平台仍用原 loginUrl 跳转后看是否仍在登录页
          let isStillOnLoginPage = false;
          try {
            const checkUrl = targetPlatform === 'kuaishou'
              ? 'https://cp.kuaishou.com/profile'
              : config.loginUrl;
            await record.page.goto(checkUrl, { waitUntil: 'domcontentloaded', timeout: 15000 });
            await record.page.waitForTimeout(3000);
            if (targetPlatform === 'kuaishou') {
              // 快手：看右上角用户状态栏是否存在
              const { HumanActions } = await import('@social-media/browser-core');
              const isLoggedIn = await HumanActions.cdpIsElementVisible(record.page, '.user-info-dpd');
              isStillOnLoginPage = !isLoggedIn;
            } else {
              const tabUrl = record.page.url();
              isStillOnLoginPage = tabUrl.includes('login') || tabUrl.includes('passport');
            }
          } catch { /* navigation failed, assume still on login */ isStillOnLoginPage = true; }
```

- [ ] **Step 3: 类型检查**

Run: `cd apps/ts-api-gateway && npx tsc --noEmit`
Expected: 无新增错误（`HumanActions` 已是该文件可用的导入；若未导入则改为顶部已存在的导入路径）

- [ ] **Step 4: 提交**

```bash
git add apps/ts-api-gateway/src/services/wechatBotService.ts
git commit -m "fix(继续监控): 快手导航到 profile 页用 .user-info-dpd 判断登录态"
```

---

## Task 7: 后端 `recoverLogin` 导出 + 路由

**Files:**
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts`（新增 `recoverLogin` 导出，紧邻 `markLoginRecovered:764`）
- Modify: `apps/ts-api-gateway/src/routes/matrix.ts`（新增路由，紧邻 `probe-login:1173`）
- Test: `apps/ts-api-gateway/src/services/__tests__/recoverLogin.test.ts`（新）

- [ ] **Step 1: 写失败测试**

创建 `apps/ts-api-gateway/src/services/__tests__/recoverLogin.test.ts`：

```typescript
import { describe, it, expect, jest } from '@jest/globals';

jest.mock('@social-media/shared-config', () => ({
  isProduction: jest.fn().mockReturnValue(false),
  isDevelopment: jest.fn().mockReturnValue(false),
  getConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  loadConfig: jest.fn().mockReturnValue({ NODE_ENV: 'test' }),
  PlatformName: 'douyin',
}));

jest.mock('../../lib/logger', () => ({
  createLogger: () => ({ error: jest.fn(), warn: jest.fn(), info: jest.fn(), debug: jest.fn() }),
}));

const mockPrismaUpdate = jest.fn();
jest.mock('../../lib/prisma', () => ({
  prisma: { platformAccount: { update: mockPrismaUpdate, findUnique: jest.fn() } },
}));

jest.mock('../loginFlowHelpers', () => ({
  getFlowIdsForPlatform: jest.fn().mockReturnValue(['creator']),
}));

const mockRedisDel = jest.fn();
jest.mock('../../lib/redis', () => ({
  getRedis: () => ({ del: mockRedisDel, get: jest.fn(), set: jest.fn() }),
}));

jest.mock('../wechatBotService', () => ({ botManager: { sendLoginAlert: jest.fn() } }));

import { recoverLogin } from '../monitorService';

describe('recoverLogin', () => {
  beforeEach(() => jest.clearAllMocks());

  it('置 status=active + cooldownUntil=0 + 清 flowState', async () => {
    const result = await recoverLogin(11, 'kuaishou', 'win-1');
    expect(mockPrismaUpdate).toHaveBeenCalledWith({
      where: { id: 11 },
      data: { status: 'active', cooldownUntil: BigInt(0) },
    });
    expect(mockRedisDel).toHaveBeenCalledWith('login_flow_state:11:creator');
    expect(result).toEqual({ message: '已置为已登录并启动监控' });
  });
});
```

> 注：`recoverLogin` 内部调用 `resetSchedulerTimer`（同模块函数，不需 mock）。测试不验证 `resetSchedulerTimer` 副作用（它操作内存 schedulerState，无外部 IO）。

- [ ] **Step 2: 运行测试验证失败**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/recoverLogin.test.ts`
Expected: FAIL — `recoverLogin is not a function`

- [ ] **Step 3: 实现 `recoverLogin`**

在 `apps/ts-api-gateway/src/services/monitorService.ts` 的 `markLoginRecovered` 函数之后（约 `:774` 闭括号后）新增：

```typescript
/**
 * 恢复登录（前端"恢复登录"按钮）：直接置 active + 清 flowState + 启动倒计时。
 * 不连浏览器、不检测、不发卡片。信任用户已扫码登录。
 * 若实际未登录，下轮监控 runXxxCheck 发现失效会发一张卡片。
 */
export async function recoverLogin(userId: number, platform: string, windowId: string): Promise<{ message: string }> {
  const { prisma } = await import('../lib/prisma');
  await prisma.platformAccount.update({
    where: { id: userId },
    data: { status: 'active', cooldownUntil: BigInt(0) },
  });
  const { getFlowIdsForPlatform } = await import('./loginFlowHelpers');
  for (const fid of getFlowIdsForPlatform(platform)) {
    await delFlowState(userId, fid);
  }
  resetSchedulerTimer(windowId, platform);
  logger.info({ userId, platform, windowId }, '[recoverLogin] 已置 active 并启动 3s 倒计时');
  return { message: '已置为已登录并启动监控' };
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/recoverLogin.test.ts`
Expected: PASS

- [ ] **Step 5: 新增路由**

在 `apps/ts-api-gateway/src/routes/matrix.ts` 的 `probe-login` 路由之后（约 `:1197` 闭括号后）新增。先确认 import 行（`:12`）含 `triggerLoginProbe`，追加 `recoverLogin`：

修改 `:12`：
```typescript
import { getAllSchedulerStatuses, resetSchedulerTimer, restartMonitorScheduler, markJobCancelled, cancelledJobIds, triggerLoginProbe, recoverLogin } from '../services/monitorService';
```

新增路由：
```typescript
/** POST /api/v1/matrix/monitor/accounts/:userId/recover-login — 直接置已登录 + 启动倒计时（不检测不发卡片） */
router.post('/monitor/accounts/:userId/recover-login', async (req: Request, res: Response) => {
  try {
    const userId = parseInt(String(req.params.userId), 10);
    if (isNaN(userId)) return res.status(400).json({ error: '无效的 userId' });

    const account = await prisma.platformAccount.findUnique({
      where: { id: userId },
      select: { platform: true, window: { select: { externalId: true } } },
    });
    if (!account) return res.status(404).json({ error: '未找到账号' });
    const windowId = account.window?.externalId ? String(account.window.externalId) : '';
    if (!windowId) return res.status(400).json({ error: '账号未关联浏览器窗口' });

    const result = await recoverLogin(userId, account.platform, windowId);
    res.json({ message: result.message, userId, platform: account.platform });
  } catch (err: any) {
    logger.error({ err: err.message }, '[recover-login] 失败');
    res.status(500).json({ error: err.message || '恢复失败' });
  }
});
```

- [ ] **Step 6: 类型检查**

Run: `cd apps/ts-api-gateway && npx tsc --noEmit`
Expected: 无新增错误

- [ ] **Step 7: 提交**

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts apps/ts-api-gateway/src/routes/matrix.ts apps/ts-api-gateway/src/services/__tests__/recoverLogin.test.ts
git commit -m "feat: 新增 recoverLogin 直接置 active+启动倒计时，前端恢复登录按钮调用"
```

---

## Task 8: 前端"恢复登录"按钮改用 `useRecoverLogin`

**Files:**
- Modify: `apps/admin-dashboard/src/hooks/useApi.ts:543`（新增 hook）
- Modify: `apps/admin-dashboard/src/app/matrix/page.tsx:1704-1720`

- [ ] **Step 1: 新增 `useRecoverLogin` hook**

在 `apps/admin-dashboard/src/hooks/useApi.ts` 的 `useProbeLogin`（`:531-543`）之后新增：

```typescript
export function useRecoverLogin() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: (userId: number) =>
      api.post(`/matrix/monitor/accounts/${userId}/recover-login`).then((r) => r.data),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ['monitor'] });
    },
  });
}
```

- [ ] **Step 2: 修改"恢复登录"按钮**

在 `apps/admin-dashboard/src/app/matrix/page.tsx`：
- `:856` 附近 `const probeLogin = useProbeLogin();` 之后新增 `const recoverLogin = useRecoverLogin();`（确认 `useRecoverLogin` 已 import）
- 修改 `:1704-1720` 按钮，把 `onClick` 内 `probeLogin.mutateAsync` 改为 `recoverLogin.mutateAsync`，Toast 文案改为"已置为已登录，3秒后开始监控"：

```tsx
                                          {isLoginRequired && (
                                            <button
                                              onClick={async () => {
                                                try {
                                                  await recoverLogin.mutateAsync(account.id);
                                                  addToast('已置为已登录，3秒后开始监控', 'success');
                                                } catch (e: any) {
                                                  addToast(e?.response?.data?.error || '恢复失败', 'error');
                                                }
                                              }}
                                              disabled={recoverLogin.isPending}
                                              className="flex items-center justify-center gap-1 px-2 py-1.5 rounded-lg text-[11px] font-medium bg-orange-500/10 text-orange-500 hover:bg-orange-500/20 transition-colors disabled:opacity-30"
                                            >
                                              <MaterialIcon icon={recoverLogin.isPending ? 'pending' : 'login'} size="xs" />
                                              {recoverLogin.isPending ? '恢复中…' : '恢复登录'}
                                            </button>
                                          )}
```

并更新 import 行：把 `useProbeLogin` 同处的 import 补上 `useRecoverLogin`（用 grep 定位 import 行：`grep -n "useProbeLogin" apps/admin-dashboard/src/app/matrix/page.tsx`）。

- [ ] **Step 3: 类型检查 + 构建**

Run: `cd apps/admin-dashboard && npx tsc --noEmit`
Expected: 无新增错误

- [ ] **Step 4: 提交**

```bash
git add apps/admin-dashboard/src/hooks/useApi.ts apps/admin-dashboard/src/app/matrix/page.tsx
git commit -m "feat(admin): 恢复登录按钮改用 recover-login 直接置 active+启动倒计时"
```

---

## Task 9: 全量回归 + Docker 重建验收

**Files:** 无（验证步骤）

- [ ] **Step 1: browser-core 单测**

Run: `cd packages/browser-core && node --test`
Expected: 全部 PASS

- [ ] **Step 2: ts-api-gateway 单测**

Run: `cd apps/ts-api-gateway && npx vitest run`
Expected: 全部 PASS（含新增 6 个测试文件 + 既有 `markLoginRecovered.test.ts`）

- [ ] **Step 3: 类型检查**

Run: `cd apps/ts-api-gateway && npx tsc --noEmit && cd ../../apps/admin-dashboard && npx tsc --noEmit`
Expected: 无新增错误

- [ ] **Step 4: Docker 重建**

Run: `docker compose up -d --build sm-ts-api`
Expected: 容器启动成功

- [ ] **Step 5: 实测验收（5 条）**

1. **快手误判**：快手账号扫码登录后，点企微卡片"继续监控" → 日志应显示导航到 `cp.kuaishou.com/profile` + 检测 `.user-info-dpd` → 标 active + 启动倒计时，不再反复探测登录页。
2. **抖音 0s**：触发抖音监控 → 日志应出现 `[计时] Phase1 开始` + `Phase1 完成` + 真实 elapsed，`monitor-db` 基于真实采集，不再 40ms fast-fail。
3. **恢复登录按钮**：login_required 状态下点"恢复登录" → 卡片立即变 active → 倒计时 3s → 3s 后自动跑一轮监控；不连浏览器、无企微卡片。
4. **快手滥发卡片**：观察快手自动轮询日志，正常登录状态下不再出现 `handleLogin` 跳 passport 页 + 阻塞 5min；失效时只发一张卡片 + 标 login_required 停调度。
5. **视频号恢复**：视频号 login_required 时点"恢复登录" → 直接置 active + 倒计时，不再因 `handleLogin` 阻塞 120s 卡死。

- [ ] **Step 6: 提交验收记录**

```bash
git add -A
git commit -m "test: 全量回归通过（快手/抖音/视频号登录态检测与恢复按钮修复）"
```

---

## 自审记录

**Spec 覆盖**：
- Bug 1（快手误判）→ Task 1（detectKuaishouLogin）+ Task 6（继续监控 goto profile）
- Bug 2（抖音 0s）→ Task 3
- Bug 3（恢复登录按钮）→ Task 7（后端）+ Task 8（前端）
- Bug 4（快手滥发卡片）→ Task 2（Phase0 移除 handleLogin）
- 视频号同步（spec D）→ Task 4 + Task 5
- 回复路径保留阻塞（spec D 修正）→ Task 4 仅新增 detectTencentLogin，不改 `:2269` 回复路径

**类型一致性**：
- `detectKuaishouLogin(page): Promise<boolean>` — Task 1 定义，Task 2/6 调用，签名一致
- `detectTencentLogin(page): Promise<boolean>` — Task 4 定义，Task 5 调用，签名一致
- `recoverLogin(userId, platform, windowId): Promise<{ message: string }>` — Task 7 定义，路由 + 测试调用，签名一致
- `useRecoverLogin()` — Task 8 定义，返回 `useMutation`，与 `useProbeLogin` 模式一致

**无占位符**：所有步骤含完整代码与命令。
