# 登录注册表串号与监控并发修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 6 个回归 bug —— `LoginTabRegistry` key 缺 platform 维度导致三平台共用窗口串号（#1/#2/#5/#6），同 (window,platform) 监控并发互斥缺口（#3），与超时过长（#4）。

**Architecture:** 三组独立改动按 A→C→B 顺序提交。改动 A 给注册表 key 与 localStorage 标记加 platform 维度，并在 `find` 内存命中后用 `record.domain === config.domain` 校验拒绝串号。改动 C 给 `captureQR` 加 URL hostname 前置校验作防御纵深并收敛 timeout。改动 B 在 `enqueueMonitor` 内部实现 per-(windowId,platform) 去重，给 Phase1 加路径级 wrong-page fast-fail，并修正 `triggerLoginProbe` 在监控活跃时直接返回。

**Tech Stack:** TypeScript、patchright（CDP）、BullMQ（per-window 队列）、Vitest/Jest（`packages/browser-core/src/__tests__` 用 `require` + `describe/it/expect`，运行 `node --test` 或项目 runner）。

**关键事实（oracle 已核实）：**
- `packages/browser-core/package.json:6` `"main": "./src/index.ts"` —— 运行时直引 TS 源，改 `.ts` 立即生效，**无需编译**。
- `LoginTabRegistry` 5 个方法签名：`register/unregister/find/openLoginTab/closeLoginTab`，均缺 platform 维度。
- `LoginTabRecord` interface 在 `packages/browser-core/src/types.ts:110-119`。
- 10 个直接调用点见 Task 1 / Task 3 清单。
- `enqueueMonitor` 在 `apps/ts-api-gateway/src/services/unifiedQueue.ts:509-525`，返回 `Promise<Job>`，当前无去重。
- `triggerLoginProbe` 在 `monitorService.ts:765`，签名 `Promise<void>`，需改为返回值类型。
- 现有测试：`packages/browser-core/src/__tests__/loginTabRegistry.test.ts`、`captureQRSelectors.test.ts`；`apps/ts-api-gateway/src/services/__tests__/monitorService.test.ts`、`loginFlowHelpers.test.ts`。

---

## File Structure

| 文件 | 责任 | 改动组 |
|---|---|---|
| `packages/browser-core/src/types.ts:110-119` | `LoginTabRecord` 加 `platform` 字段 | A |
| `packages/browser-core/src/loginTabRegistry.ts` | key 加 platform 维度、find domain 校验、captureQR hostname 前置校验、timeout 收敛 | A, C |
| `apps/ts-api-gateway/src/services/loginFlowHelpers.ts` | `ensureLoginTab` 传 platform、reuse 路径排除异平台 | A |
| `apps/ts-api-gateway/src/services/wechatBotService.ts` | 6 处 `loginTabRegistry.*` 调用传 platform | A |
| `apps/ts-api-gateway/src/services/monitorService.ts` | `triggerLoginProbe` 调用传 platform + 改返回签名；调度去重 key 升级；3 个 `runXxxCheck` Phase1 fast-fail | A, B |
| `apps/ts-api-gateway/src/crawlers/xiaohongshuCrawler.ts:1266` | `register` 调用传 platform | A |
| `apps/ts-api-gateway/src/services/unifiedQueue.ts:509` | `enqueueMonitor` 内部 per-(windowId,platform) 去重 | B |
| `apps/ts-api-gateway/src/routes/matrix.ts:1188` | probe-login 路由处理新返回值 | B |
| `apps/admin-dashboard/src/hooks/useApi.ts:531` | `useProbeLogin` 处理 `monitor_active` 提示 | B |
| 测试文件（见各 Task） | TDD | A, B, C |

---

## 改动 A：注册表 key 加 platform 维度（修 #1/#2/#5/#6）

### Task 1: `LoginTabRecord` 加 platform 字段 + key 格式升级

**Files:**
- Modify: `packages/browser-core/src/types.ts:110-119`
- Modify: `packages/browser-core/src/loginTabRegistry.ts:13-16`（register）、`:19`（unregister）、`:67`（find）、`:241`（openLoginTab）、`:278`（closeLoginTab）
- Test: `packages/browser-core/src/__tests__/loginTabRegistry.test.ts`

- [ ] **Step 1: 写失败测试 — key 含 platform，三平台同 flowId 不撞 key**

在 `packages/browser-core/src/__tests__/loginTabRegistry.test.ts` 末尾 `describe` 内追加：

```javascript
it('should isolate same flowId across platforms under shared window', () => {
  const douyin = {
    page: mockPage('https://creator.douyin.com/creator-micro/home', 't-dy'),
    targetId: 't-dy', domain: 'creator.douyin.com', flowId: 'creator',
    platform: 'douyin',
    openedAt: Date.now(), userId: 6,
    loginUrl: 'https://creator.douyin.com/creator-micro/home',
  };
  const tencent = {
    page: mockPage('https://channels.weixin.qq.com/login.html', 't-tx'),
    targetId: 't-tx', domain: 'channels.weixin.qq.com', flowId: 'creator',
    platform: 'tencent',
    openedAt: Date.now(), userId: 13,
    loginUrl: 'https://channels.weixin.qq.com/login.html',
  };
  // 同 windowId、同 flowId='creator'、不同 platform
  registry.register('w4', 'douyin', 'creator', douyin);
  registry.register('w4', 'tencent', 'creator', tencent);
  expect(registry.tabs.has('w4:douyin:creator')).toBe(true);
  expect(registry.tabs.has('w4:tencent:creator')).toBe(true);
  // 互不覆盖
  expect(registry.tabs.get('w4:douyin:creator').userId).toBe(6);
  expect(registry.tabs.get('w4:tencent:creator').userId).toBe(13);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test packages/browser-core/src/__tests__/loginTabRegistry.test.ts`（若项目用 vitest 则 `npx vitest run packages/browser-core/src/__tests__/loginTabRegistry.test.ts`）
Expected: FAIL —— `registry.register` 当前是 3 参签名，传 4 参报错或 key 仍是 `w4:creator`。

- [ ] **Step 3: 改 `LoginTabRecord` interface 加 platform 字段**

`packages/browser-core/src/types.ts:110-119` 改为：

```typescript
export interface LoginTabRecord {
  page: any; // Page (避免循环引用的 any 类型)
  targetId: string;
  domain: string;
  flowId: string;
  /** 平台标识，用于 key 隔离（同 windowId 下三平台 flowId 都是 creator 不撞 key） */
  platform: string;
  openedAt: number;
  userId: number;
  /** 登录页 URL（用于 unregister 时判定是否发生跨域跳转） */
  loginUrl: string;
}
```

- [ ] **Step 4: 改 `loginTabRegistry.ts` register/unregister/closeLoginTab 签名与 key**

`register`（`:13-16`）改为：

```typescript
  register(windowId: string, platform: string, flowId: string, record: LoginTabRecord): void {
    const key = `${windowId}:${platform}:${flowId}`;
    this.tabs.set(key, record);
  }
```

`unregister`（`:19`）签名改为 `async unregister(windowId: string, platform: string, flowId: string): Promise<void>`，内部 `const key = `${windowId}:${platform}:${flowId}`;`（替换原 `:20` 的 `const key = ${windowId}:${flowId}`）。

`closeLoginTab`（`:278-284`）签名改为 `async closeLoginTab(windowId: string, platform: string, flowId: string): Promise<void>`，内部两处 key 与 `unregister` 调用同步加 platform。

- [ ] **Step 5: 运行 Task 1 测试确认通过**

Run: `node --test packages/browser-core/src/__tests__/loginTabRegistry.test.ts`
Expected: 新测试 PASS。但旧测试（用 3 参 `register('window123','mainsite',record)`）会 FAIL —— 留到 Task 3 统一修调用方。本步只确认新测试通过。

- [ ] **Step 6: Commit**

```bash
git add packages/browser-core/src/types.ts packages/browser-core/src/loginTabRegistry.ts packages/browser-core/src/__tests__/loginTabRegistry.test.ts
git commit -m "fix(browser-core): LoginTabRegistry key 加 platform 维度防串号

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: `find` domain 校验 + 枚举兜底 platform 匹配

**Files:**
- Modify: `packages/browser-core/src/loginTabRegistry.ts:67-110`（find）、`:241-275`（openLoginTab 标记 JSON）
- Test: `packages/browser-core/src/__tests__/loginTabRegistry.test.ts`

- [ ] **Step 1: 写失败测试 — find 内存命中校验 domain 拒绝串号**

追加测试：

```javascript
it('should reject stale memory hit whose domain mismatches config', async () => {
  // 模拟旧串号残留：key 命中但 record 是抖音 page，config.domain 是视频号
  const dyPage = mockPage('https://creator.douyin.com/creator-micro/home', 't-dy');
  const staleRecord = {
    page: dyPage, targetId: 't-dy',
    domain: 'creator.douyin.com', flowId: 'creator', platform: 'douyin',
    openedAt: Date.now(), userId: 6,
    loginUrl: 'https://creator.douyin.com/creator-micro/home',
  };
  // 直接污染内存（模拟跨平台 key 冲突后的残留）
  registry.tabs.set('w4:tencent:creator', staleRecord);

  const browser = { contexts: () => [{ pages: () => [] }] };
  const found = await registry.find('w4', 'tencent', 'creator', browser, 'channels.weixin.qq.com');
  // domain 不符 → 拒绝内存命中，返回 null（无枚举兜底可用）
  expect(found).toBe(null);
  // 残留被清理
  expect(registry.tabs.has('w4:tencent:creator')).toBe(false);
});

it('should match platform in localStorage mark during enumeration fallback', async () => {
  const txPage = mockPage('https://channels.weixin.qq.com/login.html', 't-tx');
  txPage.evaluate = ({ markKey }) => Promise.resolve(JSON.stringify({
    flowId: 'creator', platform: 'tencent', userId: 13,
    openedAt: 123, loginUrl: 'https://channels.weixin.qq.com/login.html',
  }));
  const dyPage = mockPage('https://creator.douyin.com/creator-micro/home', 't-dy');
  dyPage.evaluate = ({ markKey }) => Promise.resolve(JSON.stringify({
    flowId: 'creator', platform: 'douyin', userId: 6,
    openedAt: 123, loginUrl: 'https://creator.douyin.com/creator-micro/home',
  }));
  const browser = { contexts: () => [{ pages: () => [dyPage, txPage] }] };
  // 查视频号：URL 含 channels.weixin.qq.com 的页才进入枚举；dyPage URL 不含视频号 domain 被跳过
  const found = await registry.find('w4', 'tencent', 'creator', browser, 'channels.weixin.qq.com');
  expect(found).not.toBe(null);
  expect(found.userId).toBe(13);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test packages/browser-core/src/__tests__/loginTabRegistry.test.ts`
Expected: FAIL —— 当前 find 内存命中不校验 domain。

- [ ] **Step 3: 改 `find` 签名加 platform + domain 校验 + 枚举 platform 匹配**

`packages/browser-core/src/loginTabRegistry.ts:67-110` 整段 `find` 改为：

```typescript
  async find(windowId: string, platform: string, flowId: string, browser: any, domain: string): Promise<LoginTabRecord | null> {
    const key = `${windowId}:${platform}:${flowId}`;

    // 1. 查内存
    const cached = this.tabs.get(key);
    if (cached) {
      try {
        if (!cached.page.isClosed()) {
          // domain 校验：防止跨平台 key 冲突后的残留串号
          if (cached.domain === domain) return cached;
          console.warn(`[LoginTabRegistry] find: memory hit domain mismatch (record.domain=${cached.domain}, expected=${domain}), clearing stale entry`);
        }
      } catch { /* page 引用过期 */ }
      this.tabs.delete(key);
    }

   // 2. 枚举所有页面，通过 localStorage 标记恢复
   try {
     const ctx = browser.contexts()[0];
     if (!ctx) return null;
     const pages = ctx.pages();
     for (const page of pages) {
       try {
         const url = page.url();
         if (!url.includes(domain)) continue;
         const markData = await page.evaluate(({ markKey }: { markKey: string }) => {
           const raw = localStorage.getItem(markKey);
           return raw ? JSON.parse(raw) : null;
         }, { markKey: LOGIN_TAB_MARK_KEY });
          if (markData && markData.platform === platform && markData.flowId === flowId) {
            const record: LoginTabRecord = {
              page,
              targetId: markData.targetId || '',
              domain,
              flowId,
              platform,
              openedAt: markData.openedAt || Date.now(),
              userId: markData.userId || 0,
              loginUrl: markData.loginUrl || '',
            };
           this.tabs.set(key, record);
           return record;
         }
         // 同域名但 platform/flowId 不匹配 → 跳过（可能是其他平台登录 tab 或主监控页）
       } catch { continue; }
     }
    } catch { /* browser 不可用 */ }
    return null;
  }
```

- [ ] **Step 4: 改 `openLoginTab` 签名加 platform + 标记 JSON 加 platform**

`openLoginTab`（`:241`）签名改为 `async openLoginTab(windowId: string, platform: string, userId: number, flowId: string, browser: any, config: LoginFlowConfig): Promise<LoginTabRecord | null>`。

`:254` markData 改为：

```typescript
      const markData = JSON.stringify({ flowId, platform, userId, openedAt: Date.now(), loginUrl: config.loginUrl });
```

record 创建（`:259-263`）加 `platform` 字段：

```typescript
      const record: LoginTabRecord = {
        page, targetId, domain: config.domain, flowId, platform,
        openedAt: Date.now(), userId,
        loginUrl: config.loginUrl,
      };
      this.register(windowId, platform, flowId, record);
```

- [ ] **Step 5: 运行测试确认通过**

Run: `node --test packages/browser-core/src/__tests__/loginTabRegistry.test.ts`
Expected: Task 2 两个新测试 PASS。

- [ ] **Step 6: Commit**

```bash
git add packages/browser-core/src/loginTabRegistry.ts packages/browser-core/src/__tests__/loginTabRegistry.test.ts
git commit -m "fix(browser-core): find 内存命中加 domain 校验，枚举兜底匹配 platform

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 更新所有直接调用点传 platform（10 处）

**Files:**
- Modify: `apps/ts-api-gateway/src/services/loginFlowHelpers.ts:99/127/139`
- Modify: `apps/ts-api-gateway/src/services/wechatBotService.ts:701/744/746`
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts:781/790/792`
- Modify: `apps/ts-api-gateway/src/crawlers/xiaohongshuCrawler.ts:1266`
- Test: `apps/ts-api-gateway/src/services/__tests__/loginFlowHelpers.test.ts`

- [ ] **Step 1: 改 `loginFlowHelpers.ts:ensureLoginTab`**

`:99` find 调用改为：

```typescript
  let record = await loginTabRegistry.find(windowId, platform, flowId, browser, config.domain);
```

reuse 路径排除条件（`:110-114`）改为校验 platform：

```typescript
            const mark = await page.evaluate(({ markKey }: { markKey: string }) => {
              const raw = localStorage.getItem(markKey);
              return raw ? JSON.parse(raw) : null;
            }, { markKey: '__login_tab_mark__' }).catch(() => null);
            if (mark && mark.platform === platform && mark.flowId === flowId) continue;
```

reuse 路径写标记（`:118`）与 record 创建（`:122-126`）加 platform：

```typescript
            const markData = JSON.stringify({ flowId, platform, userId, openedAt: Date.now(), loginUrl: config.loginUrl });
            await page.evaluate(({ data, markKey }: { data: string; markKey: string }) => {
              localStorage.setItem(markKey, data);
            }, { data: markData, markKey: '__login_tab_mark__' }).catch(() => {});
            record = {
              page, targetId: (page as any)._targetId || 'reused',
              domain: config.domain, flowId, platform,
              openedAt: Date.now(), userId, loginUrl: config.loginUrl,
            };
            loginTabRegistry.register(windowId, platform, flowId, record);
```

`:139` openLoginTab 调用改为：

```typescript
    record = await loginTabRegistry.openLoginTab(windowId, platform, userId, flowId, browser, config);
```

- [ ] **Step 2: 改 `wechatBotService.ts` 3 处**

`:701` find：

```typescript
          const record = await loginTabRegistry.find(windowId, targetPlatform, targetFlowId, browser, config.domain);
```

`:744` closeLoginTab：

```typescript
              await loginTabRegistry.closeLoginTab(windowId, targetPlatform, targetFlowId);
```

`:746` unregister：

```typescript
              await loginTabRegistry.unregister(windowId, targetPlatform, targetFlowId);
```

（`targetPlatform` 在该函数作用域内已存在，见 `:773` 上下文。）

- [ ] **Step 3: 改 `monitorService.ts:triggerLoginProbe` 3 处**

`:781` find：

```typescript
        const record = await loginTabRegistry.find(windowId, platform, fid, browser, config.domain);
```

`:790` closeLoginTab：

```typescript
            await loginTabRegistry.closeLoginTab(windowId, platform, fid);
```

`:792` unregister：

```typescript
            await loginTabRegistry.unregister(windowId, platform, fid);
```

（`platform` 是 `triggerLoginProbe` 的入参，见 `:765`。）

- [ ] **Step 4: 改 `xiaohongshuCrawler.ts:1266`**

`:1256` markData 加 platform：

```typescript
            const markData = JSON.stringify({ flowId: 'mainsite', platform: 'xiaohongshu', userId, openedAt: Date.now() });
```

`:1261-1265` record 加 platform 字段：

```typescript
            const record = {
              page: newPage, targetId: (newPage as any)._targetId || 'unknown',
              domain: mainsiteConfig.domain, flowId: 'mainsite', platform: 'xiaohongshu',
              openedAt: Date.now(), userId,
            };
            loginTabRegistry.register(xhsWindowId, 'xiaohongshu', 'mainsite', record);
```

- [ ] **Step 5: 修 `loginTabRegistry.test.ts` 旧测试的 3 参调用**

旧测试用 `registry.register('window123', 'mainsite', record)`，全部改为 4 参并给 record 加 `platform` 字段。例：

```javascript
    registry.register('window123', 'xiaohongshu', 'mainsite', { ...record, platform: 'xiaohongshu' });
    // key 断言改为
    expect(registry.tabs.has('window123:xiaohongshu:mainsite')).toBe(true);
```

对 `'should register and find login tab in memory'`、`'should unregister and remove from memory'`、`'should handle multiple flowIds for same window independently'` 三处统一改。`unregister` 调用同步加 platform：`registry.unregister('window123', 'xiaohongshu', 'mainsite')`。

- [ ] **Step 6: 运行 browser-core 测试套件确认全绿**

Run: `node --test packages/browser-core/src/__tests__/loginTabRegistry.test.ts packages/browser-core/src/__tests__/captureQRSelectors.test.ts`
Expected: PASS（captureQRSelectors 测试不直接调 register/find，应不受影响；若失败检查 mockPage 是否需补 platform）。

- [ ] **Step 7: 运行 loginFlowHelpers 测试**

Run: `npx vitest run apps/ts-api-gateway/src/services/__tests__/loginFlowHelpers.test.ts`（或项目 runner）
Expected: PASS。若旧测试 mock 了 `loginTabRegistry.find` 的 3 参签名，同步改为 4 参。

- [ ] **Step 8: 类型检查**

Run: `cd apps/ts-api-gateway && npx tsc --noEmit`（或 monorepo 根 `npx tsc -b`）
Expected: 无类型错误。若有，定位是哪处调用点漏传 platform。

- [ ] **Step 9: Commit**

```bash
git add apps/ts-api-gateway/src/services/loginFlowHelpers.ts apps/ts-api-gateway/src/services/wechatBotService.ts apps/ts-api-gateway/src/services/monitorService.ts apps/ts-api-gateway/src/crawlers/xiaohongshuCrawler.ts packages/browser-core/src/__tests__/loginTabRegistry.test.ts
git commit -m "fix: 10 处 LoginTabRegistry 调用点传 platform 维度

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 改动 C：captureQR hostname 前置校验 + timeout 收敛（修 #4）

### Task 4: `captureQR` URL hostname 前置校验 + waitForSelector 收敛

**Files:**
- Modify: `packages/browser-core/src/loginTabRegistry.ts:139-238`（captureQR）
- Test: `packages/browser-core/src/__tests__/captureQRSelectors.test.ts`

- [ ] **Step 1: 写失败测试 — 域名不符快速返回 null 不兜底全页**

在 `captureQRSelectors.test.ts` 追加：

```javascript
describe('captureQR - hostname guard', () => {
  let registry;
  beforeEach(() => { registry = new LoginTabRegistry(); });

  it('should return null without screenshot when page hostname mismatches config', async () => {
    let screenshotCalled = false;
    const page = {
      url: () => 'https://creator.douyin.com/creator-micro/home', // 抖音页
      close: () => Promise.resolve(),
      evaluate: () => Promise.resolve(),
      $: () => Promise.resolve(null),
      isClosed: () => false,
      screenshot: () => { screenshotCalled = true; return Promise.resolve(Buffer.from('should-not-happen')); },
      waitForSelector: () => Promise.resolve(null),
      waitForTimeout: () => Promise.resolve(),
      mainFrame: () => ({ url: () => 'https://creator.douyin.com/creator-micro/home' }),
      frames: () => [],
      mouse: { click: () => Promise.resolve() },
    };
    const config = { domain: 'channels.weixin.qq.com', qrSelectors: ['img.qrcode'] };
    const buf = await registry.captureQR(page, config);
    expect(buf).toBe(null);
    expect(screenshotCalled).toBe(false);
  });

  it('should proceed when hostname matches config', async () => {
    const page = {
      url: () => 'https://channels.weixin.qq.com/login.html',
      close: () => Promise.resolve(),
      evaluate: () => Promise.resolve(),
      $: () => Promise.resolve({ isVisible: () => Promise.resolve(false) }),
      isClosed: () => false,
      screenshot: () => Promise.resolve(Buffer.from('qr-ok')),
      waitForSelector: () => Promise.resolve({ boundingBox: () => Promise.resolve({ x: 0, y: 0, width: 200, height: 200 }) }),
      waitForTimeout: () => Promise.resolve(),
      mainFrame: () => ({ url: () => 'https://channels.weixin.qq.com/login.html' }),
      frames: () => [],
      mouse: { click: () => Promise.resolve() },
    };
    const config = { domain: 'channels.weixin.qq.com', qrSelectors: ['img.qrcode'] };
    const buf = await registry.captureQR(page, config);
    expect(buf).not.toBe(null);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test packages/browser-core/src/__tests__/captureQRSelectors.test.ts`
Expected: FAIL —— 当前 captureQR 不校验 hostname，会跑选择器后兜底全页 screenshot。

- [ ] **Step 3: 改 `captureQR` 加 hostname 前置校验**

在 `captureQR`（`:139` 方法体开头，`const selectors = config.qrSelectors || [];` 之前）插入：

```typescript
  async captureQR(page: any, config: LoginFlowConfig): Promise<Buffer | null> {
    // 前置 hostname 校验：page 当前域名与 config.loginUrl 域名不符 → 快速返回 null，不跑选择器、不兜底全页
    // 防止跨平台串号时在错误 page 上累积数十秒 waitForSelector 后发出错误全页截图
    try {
      const pageHost = new URL(page.url()).hostname;
      const loginHost = new URL(config.loginUrl).hostname;
      if (pageHost !== loginHost) {
        console.warn(`[LoginTabRegistry] captureQR: page hostname "${pageHost}" !== loginUrl hostname "${loginHost}", skip capture`);
        return null;
      }
    } catch {
      // URL 解析失败（about:blank 等）→ 不阻塞，继续尝试
    }

    const selectors = config.qrSelectors || [];
```

- [ ] **Step 4: 改 `waitForSelector` 8000→3000**

`loginTabRegistry.ts:204`：

```typescript
          const el = await frame.waitForSelector(sel, { timeout: 3000, state: 'visible' });
```

- [ ] **Step 5: 运行测试确认通过**

Run: `node --test packages/browser-core/src/__tests__/captureQRSelectors.test.ts`
Expected: 两个新测试 PASS。若旧 captureQR 测试因 hostname 不符被拦截，检查旧测试 mock 的 `page.url()` 是否与 config.domain/loginUrl 同 host——`captureQRSelectors.test.ts` 现有 mock 用 `https://channels.weixin.qq.com/login.html`，config.domain 也是 `channels.weixin.qq.com`，应放行。若旧测试失败，给其 config 补 `loginUrl: 'https://channels.weixin.qq.com/login.html'`。

- [ ] **Step 6: 改 `openLoginTab` goto 30000→12000**

`loginTabRegistry.ts:252`：

```typescript
      await page.goto(config.loginUrl, { waitUntil: 'domcontentloaded', timeout: 12000 });
```

- [ ] **Step 7: 类型检查 + 全量 browser-core 测试**

Run: `cd packages/browser-core && npx tsc --noEmit && node --test src/__tests__/`
Expected: 全绿。

- [ ] **Step 8: Commit**

```bash
git add packages/browser-core/src/loginTabRegistry.ts packages/browser-core/src/__tests__/captureQRSelectors.test.ts
git commit -m "fix(browser-core): captureQR hostname 前置校验 + timeout 收敛 (8s→3s, goto 30s→12s)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 改动 B：监控 per-(window,platform) 互斥 + Phase1 fast-fail（修 #3）

### Task 5: `enqueueMonitor` 内部 per-(windowId,platform) 去重

**Files:**
- Modify: `apps/ts-api-gateway/src/services/unifiedQueue.ts:509-525`
- Test: `apps/ts-api-gateway/src/services/__tests__/monitorService.test.ts`（或新建 `unifiedQueueDedup.test.ts`）

- [ ] **Step 1: 写失败测试 — 同 (window,platform) 二次入队被拒**

在 `monitorService.test.ts` 或新建 `apps/ts-api-gateway/src/services/__tests__/unifiedQueueDedup.test.ts`：

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';

vi.mock('../lib/redis', () => ({
  getRedis: () => ({
    get: () => Promise.resolve(null),
    set: () => Promise.resolve('OK'),
    del: () => Promise.resolve(1),
  }),
}));

describe('enqueueMonitor per-(window,platform) dedup', () => {
  it('should reject duplicate when same window+platform job is active', async () => {
    // mock getWindowQueue 返回带 active job 的队列
    const activeJob = { data: { userId: 6, platform: 'tencent', windowId: 'w4' }, isActive: () => Promise.resolve(true), isWaiting: () => Promise.resolve(false) };
    const q = {
      getJobs: (states: string[]) => Promise.resolve(states.includes('active') ? [activeJob] : []),
      add: vi.fn().mockResolvedValue({ id: 'new' }),
    };
    const mod = await import('../services/unifiedQueue');
    (mod as any).getWindowQueue = async () => q;
    const result = await mod.enqueueMonitor({
      taskId: 'mon_dup', userId: 13, platform: 'tencent' as any,
      windowId: 'w4', windowExternalId: 'w4',
    });
    expect(result).toEqual({ enqueued: false, reason: 'duplicate' });
    expect(q.add).not.toHaveBeenCalled();
  });

  it('should enqueue when no active same window+platform job', async () => {
    const q = {
      getJobs: () => Promise.resolve([]),
      add: vi.fn().mockResolvedValue({ id: 'new' }),
    };
    const mod = await import('../services/unifiedQueue');
    (mod as any).getWindowQueue = async () => q;
    const result = await mod.enqueueMonitor({
      taskId: 'mon_ok', userId: 13, platform: 'tencent' as any,
      windowId: 'w4', windowExternalId: 'w4',
    });
    expect(result.enqueued).toBe(true);
    expect(q.add).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run apps/ts-api-gateway/src/services/__tests__/unifiedQueueDedup.test.ts`
Expected: FAIL —— 当前 `enqueueMonitor` 返回 `Promise<Job>`，无去重。

- [ ] **Step 3: 改 `enqueueMonitor` 加去重 + 改返回类型**

`unifiedQueue.ts:509-525` 改为：

```typescript
export type EnqueueMonitorResult = { enqueued: true; job: Job } | { enqueued: false; reason: 'duplicate' };

export async function enqueueMonitor(task: {
  taskId: string;
  userId: number;
  platform: PlatformName;
  windowId: string;
  windowExternalId: string;
}, options?: { jobId?: string }): Promise<EnqueueMonitorResult> {
  const q = await getWindowQueue(task.windowId);

  // per-(windowId, platform) 去重：同窗口同平台已有活跃/等待任务 → 拒绝入队
  const [activeJobs, waitingJobs] = await Promise.all([
    q.getJobs(['active']),
    q.getJobs(['waiting']),
  ]);
  for (const j of [...activeJobs, ...waitingJobs]) {
    const data = j.data as any;
    if (!data) continue;
    if (data.windowId === task.windowId && data.platform === task.platform) {
      if (await j.isActive() || await j.isWaiting()) {
        return { enqueued: false, reason: 'duplicate' };
      }
    }
  }

  const job = await q.add('monitor', {
    taskType: 'monitor',
    ...task,
  }, {
    jobId: options?.jobId || task.taskId,
    attempts: 2,
    backoff: { type: 'fixed', delay: 300_000 },
  });
  return { enqueued: true, job };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run apps/ts-api-gateway/src/services/__tests__/unifiedQueueDedup.test.ts`
Expected: PASS。

- [ ] **Step 5: 更新所有 `enqueueMonitor` 调用方处理新返回类型**

调用点（`grep -rn "enqueueMonitor" apps/ts-api-gateway/src apps/admin-dashboard/src`，已知 `wechatBotService.ts:729/754` 的 setTimeout 内、`matrix.ts:1227/1291`、`monitorService.ts` 调度器 `:1863`）。每处原 `await enqueueMonitor(...)` 现返回 `EnqueueMonitorResult`，需处理：

- `matrix.ts:1227/1291`（路由）：改为
  ```typescript
  const result = await enqueueMonitor({ ... });
  if (!result.enqueued) {
    return res.status(409).json({ error: '同窗口同平台已有监控任务运行中', reason: result.reason });
  }
  const job = result.job;
  ```
- `wechatBotService.ts:729/754`（setTimeout 内重试）：忽略返回值即可（`await enqueueMonitor(...)` 仍合法，返回值不使用），但需确认无 `const job = await enqueueMonitor(...)` 解构。若仅 `await` 调用无需改。
- `monitorService.ts:1863`：调度器入队，改为
  ```typescript
      const result = await enqueueMonitor({ ... });
      if (result.enqueued) queued++;
  ```

逐一 grep 确认无遗漏的 `const job = await enqueueMonitor` 解构未处理。

- [ ] **Step 6: 类型检查**

Run: `cd apps/ts-api-gateway && npx tsc --noEmit`
Expected: 无错误。若有 `Property 'job' does not exist on type 'EnqueueMonitorResult'` 类错误，定位调用方未处理 `enqueued: false` 分支。

- [ ] **Step 7: Commit**

```bash
git add apps/ts-api-gateway/src/services/unifiedQueue.ts apps/ts-api-gateway/src/services/__tests__/unifiedQueueDedup.test.ts apps/ts-api-gateway/src/routes/matrix.ts apps/ts-api-gateway/src/services/monitorService.ts apps/ts-api-gateway/src/services/wechatBotService.ts
git commit -m "fix: enqueueMonitor 内部 per-(windowId,platform) 去重防并发监控

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: 调度器层去重 key 同步升级

**Files:**
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts:1846-1854`

- [ ] **Step 1: 改调度器去重 key 为 windowId+platform**

`:1846-1854` 当前按 `userId` 收集 `activeUserIds`。改为按 `${windowId}:${platform}` 收集，与 `enqueueMonitor` 内部去重口径一致（双重防御）：

```typescript
    const activeKeys = new Set<string>();
    for (const j of [...activeJobs, ...waitingJobs]) {
      const data = j.data as any;
      if (!data?.windowId || !data?.platform) continue;
      if (await j.isActive() || await j.isWaiting()) {
        activeKeys.add(`${data.windowId}:${data.platform}`);
      }
    }

    // 入队（跳过已有同窗口同平台任务的用户）
    let queued = 0;
    for (const u of matched) {
      const key = `${u.windowExternalId}:${u.platform}`;
      if (activeKeys.has(key)) {
        logger.debug({ userId: u.id, platform: u.platform }, '[调度] 跳过：同窗口同平台已有任务');
        continue;
      }
      const result = await enqueueMonitor({
        taskId: `mon_${Date.now()}_${u.id}`,
        userId: u.id,
        platform: u.platform as PlatformName,
        windowId: u.windowExternalId,
        windowExternalId: u.windowExternalId,
      });
      if (result.enqueued) queued++;
    }

    if (queued === 0 && activeKeys.size > 0) {
```

（`:1873` 的 `activeUserIds.size` 同步改 `activeKeys.size`。）

- [ ] **Step 2: 类型检查 + monitorService 测试**

Run: `cd apps/ts-api-gateway && npx tsc --noEmit && npx vitest run src/services/__tests__/monitorService.test.ts`
Expected: 无类型错误；现有 monitorService 测试不回归（若旧测试断言按 userId 去重，更新为 window+platform 口径）。

- [ ] **Step 3: Commit**

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts
git commit -m "fix: 调度器去重 key 升级为 windowId+platform

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: `triggerLoginProbe` 监控活跃时直接返回 + 改签名

**Files:**
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts:765-810`
- Modify: `apps/ts-api-gateway/src/routes/matrix.ts:1173-1193`
- Modify: `apps/admin-dashboard/src/hooks/useApi.ts:531-540`

- [ ] **Step 1: 写失败测试 — 监控活跃时 probe 直接返回不探测**

在 `monitorService.test.ts` 追加（mock `getWindowQueue` 返回 active job）：

```typescript
describe('triggerLoginProbe monitor_active guard', () => {
  it('should return monitor_active without probing when same window+platform monitor is active', async () => {
    const activeJob = { data: { userId: 13, platform: 'tencent', windowId: 'w4' }, isActive: () => Promise.resolve(true), isWaiting: () => Promise.resolve(false) };
    vi.doMock('../services/unifiedQueue', () => ({
      getWindowQueue: async () => ({ getJobs: () => Promise.resolve([activeJob]) }),
    }));
    const { triggerLoginProbe } = await import('../services/monitorService');
    const result = await triggerLoginProbe(13, 'tencent', 'w4', undefined, true);
    expect(result).toEqual({ probed: false, reason: 'monitor_active' });
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run apps/ts-api-gateway/src/services/__tests__/monitorService.test.ts`
Expected: FAIL —— 当前签名 `Promise<void>`，无返回值。

- [ ] **Step 3: 改 `triggerLoginProbe` 签名与入口守卫**

`monitorService.ts:765` 签名与开头改为：

```typescript
export type LoginProbeResult = { probed: true } | { probed: false; reason: 'monitor_active' };

export async function triggerLoginProbe(userId: number, platform: string, windowId: string, flowId?: string, force: boolean = false): Promise<LoginProbeResult> {
  // 入口守卫：同 (windowId, platform) 有活跃监控 → 直接返回，不探测（监控完成即已登录，probe 无必要）
  try {
    const { getWindowQueue } = await import('./unifiedQueue');
    const q = await getWindowQueue(windowId);
    const [activeJobs, waitingJobs] = await Promise.all([q.getJobs(['active']), q.getJobs(['waiting'])]);
    for (const j of [...activeJobs, ...waitingJobs]) {
      const data = j.data as any;
      if (data?.windowId === windowId && data?.platform === platform) {
        if (await j.isActive() || await j.isWaiting()) {
          logger.info({ userId, platform }, '[triggerLoginProbe] 同窗口同平台监控活跃，跳过探测');
          return { probed: false, reason: 'monitor_active' };
        }
      }
    }
  } catch (err: any) {
    logger.warn({ userId, err: err.message }, '[triggerLoginProbe] 活跃监控检查失败，继续探测');
  }

  const { loginTabRegistry, getLoginFlowConfig, getFlowIdsForPlatform } = await import('./loginFlowHelpers');
  const bm = getBrowserManager();

  const flowIds = flowId ? [flowId] : getFlowIdsForPlatform(platform);
  for (const fid of flowIds) {
    // ... 原有逻辑不变 ...
  }
  return { probed: true };
}
```

（函数末尾 `return { probed: true };`，原有 `for` 循环内的 `return` 若有需改为不中断——检查 `:781-810` 原逻辑，`return` 都在 setTimeout 回调内，不影响外层。确保外层 for 正常结束返回 `{ probed: true }`。）

- [ ] **Step 4: 改 probe-login 路由处理新返回值**

`matrix.ts:1188`：

```typescript
    const result = await triggerLoginProbe(userId, account.platform, windowId, undefined, true);
    if (!result.probed && result.reason === 'monitor_active') {
      return res.json({ message: '已有监控运行中，无需探测', userId, platform: account.platform, reason: result.reason });
    }
    res.json({ message: '已触发登录探测', userId, platform: account.platform });
```

- [ ] **Step 5: 改 `useProbeLogin` 前端提示**

`useApi.ts:531-540`（确认实际行号后）在 mutate 的 onSuccess 或调用方处理 `reason === 'monitor_active'`，显示 toast「已有监控运行中」。若 `useApi` 仅封装请求不做 UI，则在前端调用 `useProbeLogin` 的组件（grep `useProbeLogin` 定位）加：

```typescript
onSuccess: (data) => {
  if (data?.reason === 'monitor_active') {
    toast({ title: '已有监控运行中', description: '请等待当前监控完成' });
  }
},
```

- [ ] **Step 6: 运行测试 + 类型检查**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/monitorService.test.ts && npx tsc --noEmit`
Expected: 新测试 PASS，无类型错误。

- [ ] **Step 7: Commit**

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts apps/ts-api-gateway/src/routes/matrix.ts apps/admin-dashboard/src/hooks/useApi.ts
git commit -m "fix: triggerLoginProbe 监控活跃时直接返回 monitor_active，不排队不探测

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: Phase1 wrong-page fast-fail（视频号/抖音/快手）

**Files:**
- Modify: `apps/ts-api-gateway/src/crawlers/tencentCrawler.ts:967-1000`（checkForUpdates 入口）
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts:1006-1009`（runDouyinCheck）、`:1180-1186`（runKuaishouCheck）
- Test: `apps/ts-api-gateway/src/services/__tests__/monitorService.test.ts`

- [ ] **Step 1: 写失败测试 — 视频号 Phase1 在评论页 fast-fail**

```typescript
describe('runTencentCheck Phase1 wrong-page fast-fail', () => {
  it('should fast-fail when page is on comment page not post list', async () => {
    const page = { url: () => 'https://channels.weixin.qq.com/platform/comment/123' };
    const task = { userId: 13, windowId: 'w4', platform: 'tencent' } as any;
    const { runTencentCheck } = await import('../services/monitorService');
    // mock getTencentCrawler / getCrawlConfig
    const result = await runTencentCheck(page, task, undefined);
    expect(result.phase).toBe('Phase1');
    expect(result.hasUpdate).toBe(false);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run apps/ts-api-gateway/src/services/__tests__/monitorService.test.ts`
Expected: FAIL —— 当前 `runTencentCheck` 无 wrong-page 检测。

- [ ] **Step 3: 视频号 Phase1 入口加路径校验**

`monitorService.ts:runTencentCheck`（`:1562` Phase1 onProgress 之前）插入：

```typescript
  // Phase 1: 检测更新（视频列表扫描 + 对比数据库）
  // wrong-page fast-fail：若被其他进程导航到评论页（/platform/comment），Phase1 等不到作品管理页 → 直接返回
  const tencentUrl = page.url();
  if (tencentUrl.includes('/platform/comment') || tencentUrl.includes('/platform/home')) {
    logger.warn({ userId: task.userId, url: tencentUrl }, '[视频号] Phase1 入口 page 偏离作品管理页，fast-fail');
    return { hasUpdate: false, newComments: 0, updatedVideos: [], phase: 'Phase1', riskDetected: false };
  }
  onProgress?.({ phase: 'Phase1', step: '扫描视频列表', percent: 20, detail: '正在获取视频列表并对比评论数' });
```

- [ ] **Step 4: 抖音 Phase1 入口加路径校验**

`monitorService.ts:runDouyinCheck`（`:1006` currentUrl 检查处之后，Phase1 onProgress 之前）插入：

```typescript
  // wrong-page fast-fail：偏离 creator-micro 主页/内容管理 → fast-fail
  const dyUrl = page.url();
  const dyOk = dyUrl.includes('/creator-micro/home') || dyUrl.includes('/creator-micro/content/manage');
  if (!dyOk) {
    logger.warn({ userId: task.userId, url: dyUrl }, '[抖音] Phase1 入口 page 偏离作品管理页，fast-fail');
    return { hasUpdate: false, newComments: 0, updatedVideos: [], phase: 'Phase1', riskDetected: false };
  }
```

- [ ] **Step 5: 快手 Phase1 入口加路径校验**

`monitorService.ts:runKuaishouCheck`（`:1180` currentUrl 检查处之后，Phase1 onProgress 之前）插入：

```typescript
  // wrong-page fast-fail：偏离视频管理页 → fast-fail
  const ksUrl = page.url();
  if (!ksUrl.includes('/article/publish/video')) {
    logger.warn({ userId: task.userId, url: ksUrl }, '[快手] Phase1 入口 page 偏离视频管理页，fast-fail');
    return { hasUpdate: false, newComments: 0, updatedVideos: [], phase: 'Phase1', riskDetected: false };
  }
```

- [ ] **Step 6: 运行测试 + 类型检查**

Run: `cd apps/ts-api-gateway && npx vitest run src/services/__tests__/monitorService.test.ts && npx tsc --noEmit`
Expected: 新测试 PASS；现有 monitorService 测试不回归（若有测试 mock 的 page.url 返回非上述路径，更新 mock 为合法路径）。

- [ ] **Step 7: Commit**

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts apps/ts-api-gateway/src/services/__tests__/monitorService.test.ts
git commit -m "fix: 三平台 Phase1 入口 wrong-page fast-fail，被导航带偏时不再挂'等待中'

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 收尾

### Task 9: 全量回归测试

- [ ] **Step 1: 全量 browser-core 测试**

Run: `cd packages/browser-core && node --test src/__tests__/`
Expected: 全绿。

- [ ] **Step 2: 全量 ts-api-gateway 测试**

Run: `cd apps/ts-api-gateway && npx vitest run`
Expected: 全绿。重点关注 `monitorService.test.ts`、`loginFlowHelpers.test.ts`、上一批 PR（4e82a81/7d051e5/38f8ab8）引入的 probe-login / 复用相关测试。

- [ ] **Step 3: 前端构建检查**

Run: `cd apps/admin-dashboard && npx tsc --noEmit`
Expected: 无错误。

- [ ] **Step 4: 验收对照 spec §6**

逐条核对 spec 验收标准 1-7：
1. 三平台共用窗口刷新发自己 QR — 代码审查 + captureQR hostname 校验单测覆盖
2. find domain 校验拒绝串号 — Task 2 测试覆盖
3. 同 (window,tencent) 不再两个并发；Phase1 被带偏 fast-fail — Task 5/8 测试覆盖
4. 卡片刷新耗时下降 — hostname 前置校验让错误 page ≤3s 返回
5. captureQR 域名不符返回 null — Task 4 测试覆盖
6. 上一批 PR 测试套件重跑通过 — Step 2 覆盖
7. 无新增裸 page.evaluate/page.mouse — grep 确认

- [ ] **Step 5: 最终提交（若有未提交修正）**

```bash
git add -A
git commit -m "test: 回归验证通过

Co-Authored-By: Claude <noreply@anthropic.com>"
```
