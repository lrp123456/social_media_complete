# 登录刷新链路与登录态恢复修复 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复登录流程全链路新建标签页无法复用、快手刷新不发 QR、视频号恢复登录无效与更新成功仍显示需重新登录三个 bug 集群。

**Architecture:** 方案 A（根因修复 + 抽共享激活逻辑）。A0 把 `find`/复用的 URL 筛选从 `domain`（监控域）改用 `loginUrl` hostname，修快手登录域≠监控域导致的复用失效；A1 抽 `activatePlatformQR` 统一 CDP 激活，强刷/F5 二次导航后重新点开 QR；A2 导航 timeout 收敛+容错；B1 监控成功后重置 active；B2 probe 找不到 tab 时回退检测。全页兜底保留。

**Tech Stack:** TypeScript、patchright CDP、vitest/Jest（`node --test` for browser-core，`npx vitest run` for ts-api-gateway）、Prisma、BullMQ、pino。

**前置事实（已 grep 核实）：**
- `packages/browser-core` 直接以 TS 源码被引用（`"main": "./src/index.ts"`），改完无需编译 dist。
- `find` 3 处调用点：`loginFlowHelpers.ts:99`、`wechatBotService.ts:701`、`monitorService.ts:801`，均传 `config.domain`。
- `openLoginTab:278` 与 `ensureLoginTab` 复用分支 `:124` 存 `domain: config.domain`（监控域）。
- `sendLoginQR` 是 `monitorService.ts:415` 顶层导出函数，与 `triggerLoginProbe`（`:767`）同文件。
- `delFlowState`（`:756`）、`setFlowState`（`:745`）、`getAllFlowStates`（`:846`）均在 monitorService 内导出。
- dispatch 入口 `monitorService.ts:976-987`。
- 快手 `domain=cp.kuaishou.com`，`loginUrl` hostname=`passport.kuaishou.com`（异域）；视频号/抖音同域。

---

## Task 1: A0 — 新增 `getLoginHost`/`isOnLoginDomain` helper + `find` 签名改 `loginHost`

**Files:**
- Modify: `apps/ts-api-gateway/src/services/loginFlowHelpers.ts`（新增 helper）
- Modify: `packages/browser-core/src/loginTabRegistry.ts:67-115`（`find` 签名 + 内存命中校验 + 枚举筛选）
- Test: `packages/browser-core/src/__tests__/loginTabRegistry.test.ts`

- [ ] **Step 1: 写失败测试 — `find` 用 loginHost 命中快手登录页**

在 `packages/browser-core/src/__tests__/loginTabRegistry.test.ts` 末尾新增（参考既有 `mockPage(url, targetId)` helper 与 `:231`/`:251` 用例风格）：

```ts
describe('find loginHost (A0)', () => {
  it('快手登录页 hostname=passport.kuaishou.com 能被 loginHost 命中（domain 异域不再跳过）', async () => {
    const registry = new LoginTabRegistry();
    const loginUrl = 'https://passport.kuaishou.com/pc/account/login/?sid=kuaishou.web.cp.api';
    const loginHost = new URL(loginUrl).hostname; // passport.kuaishou.com
    // 模拟一个已在 passport.kuaishou.com 的页面，带 localStorage 标记
    const page = mockPage(loginUrl, 't1');
    (page.evaluate as any) = async () => ({
      platform: 'kuaishou', flowId: 'creator', openedAt: Date.now(), userId: 11, loginUrl,
    });
    const browser = { contexts: () => [{ pages: () => [page] }] } as any;
    const found = await registry.find('w4', 'kuaishou', 'creator', browser, loginHost);
    if (!found) throw new Error('expected find to hit kuaishou login page via loginHost');
    if (found.page !== page) throw new Error('expected found.page === mock page');
  });

  it('内存命中校验用 page 实际 hostname，离开登录域则 miss', async () => {
    const registry = new LoginTabRegistry();
    const loginUrl = 'https://passport.kuaishou.com/pc/account/login/';
    const loginHost = new URL(loginUrl).hostname;
    const page = mockPage(loginUrl, 't2');
    (page.isClosed as any) = () => false;
    registry.register('w4', 'kuaishou', 'creator', {
      page, targetId: 't2', domain: 'cp.kuaishou.com', flowId: 'creator', platform: 'kuaishou',
      openedAt: Date.now(), userId: 11, loginUrl,
    });
    // page 仍在登录域 → 命中
    const hit = await registry.find('w4', 'kuaishou', 'creator', { contexts: () => [{ pages: () => [] }] } as any, loginHost);
    if (!hit) throw new Error('expected memory hit while on login domain');
    // page 已导航离开登录域 → 不命中（url 改为 cp.kuaishou.com）
    (page.url as any) = () => 'https://cp.kuaishou.com/home';
    const miss = await registry.find('w4', 'kuaishou', 'creator', { contexts: () => [{ pages: () => [] }] } as any, loginHost);
    if (miss) throw new Error('expected miss after page navigated off login domain');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /home/lrp/social_media_complete/packages/browser-core && node --test src/__tests__/loginTabRegistry.test.ts`
Expected: FAIL（`find` 第 5 参仍叫 `domain`，且 `:91` 用 `url.includes(domain)`，快手 `passport.kuaishou.com` 不含传入的 `passport.kuaishou.com`？——实际当前传 `config.domain=cp.kuaishou.com` 会跳过；测试传 loginHost 但实现仍按 domain 逻辑，内存命中校验也仍是 `cached.domain === domain`，第二个用例离开登录域仍会命中 → FAIL）

- [ ] **Step 3: 在 `loginFlowHelpers.ts` 新增 helper**

在 `loginFlowHelpers.ts` 顶部 import 区后（`loadLoginFlowConfig` 之前）新增：

```ts
/** 从 loginFlow 配置取登录页 hostname（登录域）。domain 字段是监控域，可能与登录域不同（如快手）。 */
export function getLoginHost(config: LoginFlowConfig): string {
  try { return new URL(config.loginUrl).hostname; } catch { return config.domain; }
}
/** 判断 page URL 是否在登录域（hostname 相等或为 .{loginHost} 子域）。 */
export function isOnLoginDomain(url: string, loginHost: string): boolean {
  try {
    const h = new URL(url).hostname;
    return h === loginHost || h.endsWith('.' + loginHost);
  } catch { return false; }
}
```

- [ ] **Step 4: 改 `find` 签名与内存命中校验**

`packages/browser-core/src/loginTabRegistry.ts:67` 签名第 5 参 `domain: string` → `loginHost: string`。`find` 内部：

`find` 内存命中段（`:71-81`）改为：
```ts
const cached = this.tabs.get(key);
if (cached) {
  try {
    if (!cached.page.isClosed()) {
      // 校验 page 当前 hostname 仍在登录域（不读 cached.domain 字段，因其存的是监控域）
      try {
        if (isOnLoginDomain(cached.page.url(), loginHost)) return cached;
        console.warn(`[LoginTabRegistry] find: memory hit but page left login domain (loginHost=${loginHost}), clearing`);
      } catch { /* page.url() 失败，保守返回 cached */ return cached; }
    }
  } catch { /* page 引用过期 */ }
  this.tabs.delete(key);
}
```

枚举段（`:88-112`）`if (!url.includes(domain)) continue;`（`:91`）改为 `if (!isOnLoginDomain(url, loginHost)) continue;`。枚举内构造 record 的 `domain` 字段（`:100`）保持存传入的 `loginHost`（与原 `domain` 参数同位语义，即调用方传入的值）——为保持 `LoginTabRecord.domain` 语义为监控域不变，改为存 `new URL(url).hostname`（实际登录域），与 `openLoginTab:278` 存 `config.domain` 区分；此处枚举恢复的 record 用 page 实际 hostname。

> 注意：`loginTabRegistry.ts` 顶部需 import `isOnLoginDomain`。但 `packages/browser-core` 不能依赖 `apps/ts-api-gateway`。故 `isOnLoginDomain` 应在 `packages/browser-core/src/loginTabRegistry.ts` 内本地实现（或放 `packages/browser-core/src/types.ts` 旁的工具）。**调整：`isOnLoginDomain` 在 `loginTabRegistry.ts` 内本地定义**，`loginFlowHelpers.ts` 的 `isOnLoginDomain` 供 gateway 侧复用（两处实现相同，或 gateway 侧 import browser-core 导出的版本）。

**最终落位**：把 `getLoginHost` 与 `isOnLoginDomain` 都放 `packages/browser-core/src/loginTabRegistry.ts` 导出（browser-core 内实现，gateway 侧 `loginFlowHelpers.ts` re-export 或直接 import）。

修订 Step 3：改为在 `packages/browser-core/src/loginTabRegistry.ts` 文件顶部（`QR_PADDING` 常量后）新增并 export：
```ts
export function getLoginHost(loginUrl: string, fallbackDomain: string): string {
  try { return new URL(loginUrl).hostname; } catch { return fallbackDomain; }
}
export function isOnLoginDomain(url: string, loginHost: string): boolean {
  try {
    const h = new URL(url).hostname;
    return h === loginHost || h.endsWith('.' + loginHost);
  } catch { return false; }
}
```
`find` 签名第 5 参仍叫 `loginHost`。gateway 侧调用点传 `getLoginHost(config.loginUrl, config.domain)`。

- [ ] **Step 5: 运行测试确认通过**

Run: `cd /home/lrp/social_media_complete/packages/browser-core && node --test src/__tests__/loginTabRegistry.test.ts`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add packages/browser-core/src/loginTabRegistry.ts packages/browser-core/src/__tests__/loginTabRegistry.test.ts
git commit -m "fix(browser-core): find 改用 loginHost 校验，快手异域登录页可复用"
```

---

## Task 2: A0 — 更新 3 处 `find` 调用点 + 测试传 loginHost

**Files:**
- Modify: `apps/ts-api-gateway/src/services/loginFlowHelpers.ts:99`
- Modify: `apps/ts-api-gateway/src/services/wechatBotService.ts:701`
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts:801`
- Modify: `apps/ts-api-gateway/src/services/loginFlowHelpers.ts:108`（复用分支 URL 筛选）

- [ ] **Step 1: 更新 `ensureLoginTab` 内 find 调用（`:99`）与复用分支筛选（`:108`）**

`loginFlowHelpers.ts:99`：
```ts
const loginHost = getLoginHost(config.loginUrl, config.domain);
let record = await loginTabRegistry.find(windowId, platform, flowId, browser, loginHost);
```
（`getLoginHost` 从 `@social-media/browser-core` import，或本地 `import { getLoginHost } from '@social-media/browser-core'`——确认 browser-core `src/index.ts` 已 re-export `getLoginHost`/`isOnLoginDomain`。若未 re-export，在 `packages/browser-core/src/index.ts` 加 `export { getLoginHost, isOnLoginDomain } from './loginTabRegistry';`。）

`loginFlowHelpers.ts:108` 复用分支：
```ts
if (!isOnLoginDomain(url, loginHost)) continue;
```
（`loginHost` 已在 `:99` 上方定义，复用。`isOnLoginDomain` 同从 browser-core import。）

- [ ] **Step 2: 更新 `wechatBotService.ts:701`**

读 `wechatBotService.ts:695-705` 上下文确认 `config` 在 scope。改为：
```ts
const loginHost = getLoginHost(config.loginUrl, config.domain);
const record = await loginTabRegistry.find(windowId, targetPlatform, targetFlowId, browser, loginHost);
```
顶部 import 补 `getLoginHost`（从 `./loginFlowHelpers` 或 `@social-media/browser-core`，与文件既有 import 风格一致）。

- [ ] **Step 3: 更新 `monitorService.ts:801`（probe 内）**

改为：
```ts
const loginHost = getLoginHost(config.loginUrl, config.domain);
const record = await loginTabRegistry.find(windowId, platform, fid, browser, loginHost);
```
顶部 import 补 `getLoginHost`。

- [ ] **Step 4: 类型检查 + 现有测试**

Run: `cd /home/lrp/social_media_complete && npx tsc -p apps/ts-api-gateway/tsconfig.json --noEmit`
Expected: 无新增错误（`find` 第 5 参仍是 string，签名兼容）。

Run: `cd /home/lrp/social_media_complete/apps/ts-api-gateway && npx vitest run src/services/__tests__/loginFlowHelpers.test.ts`
Expected: PASS（既有用例 `:85`/`:99` 调 `ensureLoginTab('...', 6, 'douyin', 'creator')`，douyin 同域，行为不变）。

- [ ] **Step 5: Commit**

```bash
git add apps/ts-api-gateway/src/services/loginFlowHelpers.ts apps/ts-api-gateway/src/services/wechatBotService.ts apps/ts-api-gateway/src/services/monitorService.ts packages/browser-core/src/index.ts
git commit -m "fix: 3 处 find 调用点传 loginHost，复用分支用 isOnLoginDomain"
```

---

## Task 3: A1 — 抽 `activatePlatformQR` 共享激活函数

**Files:**
- Modify: `apps/ts-api-gateway/src/services/loginFlowHelpers.ts`（新增 `activatePlatformQR`，替换 `:144-193` 内联逻辑）
- Modify: `packages/browser-core/src/loginTabRegistry.ts:160-196`（移除 `captureQR` 内 `qrActivationSelector` 激活分支）
- Test: `apps/ts-api-gateway/src/services/__tests__/loginFlowHelpers.test.ts`

- [ ] **Step 1: 写失败测试 — `activatePlatformQR` 对快手点 switch 后等待 QR 可见**

在 `apps/ts-api-gateway/src/services/__tests__/loginFlowHelpers.test.ts` 末尾新增（参考既有 mock 风格）：

```ts
describe('activatePlatformQR (A1)', () => {
  it('快手：点 div.platform-switch 后等待 qrSelectors 可见返回 true', async () => {
    const clicked: string[] = [];
    const page: any = {
      url: () => 'https://passport.kuaishou.com/pc/account/login/',
      $: async (sel: string) => (sel === 'div.platform-switch' ? { click: async () => { clicked.push(sel); } } : null),
      waitForSelector: async (sel: string) => {
        if (sel === 'img[alt="qrcode"]') return { /* visible */ };
        throw new Error('not found');
      },
      waitForTimeout: async () => {},
    };
    // 强制非 v2 路径
    jest.resetModules();
    jest.doMock('../../lib/antiDetectionMode', () => ({ isAntiDetectionV2: () => false }));
    const { activatePlatformQR } = await import('../loginFlowHelpers');
    const config = {
      domain: 'cp.kuaishou.com', loginUrl: 'https://passport.kuaishou.com/x',
      qrSelectors: ['img[alt="qrcode"]', 'canvas'], qrActivationSelector: undefined,
    } as any;
    const ok = await activatePlatformQR(page, 'kuaishou', config);
    if (!ok) throw new Error('expected activatePlatformQR true');
    if (clicked.length !== 1 || clicked[0] !== 'div.platform-switch') throw new Error('expected switch clicked once');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /home/lrp/social_media_complete/apps/ts-api-gateway && npx vitest run src/services/__tests__/loginFlowHelpers.test.ts`
Expected: FAIL（`activatePlatformQR` 未导出）

- [ ] **Step 3: 在 `loginFlowHelpers.ts` 新增 `activatePlatformQR`**

在 `ensureLoginTab` 函数之前新增。把原 `ensureLoginTab:144-193`（PLATFORM_QR_SWITCH 切换 + 小红书预激活）与 `captureQR:160-196`（qrActivationSelector 激活）的逻辑合并，统一 CDP/非 CDP：

```ts
/**
 * 平台 QR 激活：点击平台切换按钮（快手 div.platform-switch）、qrActivationSelector 通用激活、
 * 小红书缩略图预激活，并等待任一 qrSelectors 可见。返回是否成功激活（QR 可见）。
 * 供 ensureLoginTab 导航后、强刷/F5 二次导航后、sendLoginQR 短路分支调用。
 */
export async function activatePlatformQR(page: any, platform: string, config: LoginFlowConfig): Promise<boolean> {
  const selectors = config.qrSelectors || [];

  // 1. 平台 QR 切换按钮（快手等）
  const switchSelector = PLATFORM_QR_SWITCH[platform];
  if (switchSelector) {
    try {
      if (isAntiDetectionV2()) {
        if (await HumanActions.cdpIsElementVisible(page, switchSelector)) {
          await HumanActions.cdpClick(page, switchSelector);
          await new Promise(r => setTimeout(r, 2000));
          console.info(`[activatePlatformQR] clicked QR switch "${switchSelector}" for ${platform} (v2)`);
        }
      } else {
        const el = await page.$(switchSelector);
        if (el) { await el.click(); await new Promise(r => setTimeout(r, 2000)); console.info(`[activatePlatformQR] clicked QR switch "${switchSelector}" for ${platform}`); }
      }
    } catch { /* switch may not be needed */ }
  }

  // 2. qrActivationSelector 通用激活（原 captureQR 逻辑，统一 CDP）
  if (config.qrActivationSelector) {
    try {
      let qrAlreadyVisible = false;
      for (const sel of selectors) {
        try {
          const el = await page.$(sel);
          if (el && await el.isVisible().catch(() => false)) { qrAlreadyVisible = true; break; }
        } catch { continue; }
      }
      if (!qrAlreadyVisible) {
        if (isAntiDetectionV2()) {
          if (await HumanActions.cdpIsElementVisible(page, config.qrActivationSelector)) {
            await HumanActions.cdpClick(page, config.qrActivationSelector);
            await new Promise(r => setTimeout(r, 2000));
          }
        } else {
          const activator = await page.$(config.qrActivationSelector);
          if (activator) {
            const box = await activator.boundingBox();
            if (box) await page.mouse.click(box.x + box.width / 2, box.y + box.height / 2);
            else await activator.click();
            await page.waitForTimeout(2000);
          }
        }
      }
    } catch (err: any) { console.warn(`[activatePlatformQR] activation selector failed: ${err.message}`); }
  }

  // 3. 小红书缩略图预激活
  if (platform === 'xiaohongshu') {
    try {
      if (isAntiDetectionV2()) {
        if (!(await HumanActions.cdpIsElementVisible(page, 'div.css-dvxtzn'))) {
          if (await HumanActions.cdpIsElementVisible(page, 'img.css-wemwzq')) {
            await HumanActions.cdpClick(page, 'img.css-wemwzq');
            await new Promise(r => setTimeout(r, 2500));
          }
        }
      } else {
        const alreadyOpen = await page.$('div.css-dvxtzn');
        if (!alreadyOpen) {
          const thumb = await page.$('img.css-wemwzq');
          if (thumb) {
            const box = await thumb.boundingBox();
            if (box) await page.mouse.click(box.x + box.width / 2, box.y + box.height / 2);
            else await thumb.click();
            await new Promise(r => setTimeout(r, 2500));
          }
        }
      }
    } catch (err: any) { console.warn(`[activatePlatformQR] xiaohongshu activation failed: ${err.message}`); }
  }

  // 4. 等待任一 qrSelectors 可见
  for (const sel of selectors) {
    try { await page.waitForSelector(sel, { timeout: 3000, state: 'visible' }); return true; } catch { continue; }
  }
  return false;
}
```

- [ ] **Step 4: 替换 `ensureLoginTab:144-193` 内联逻辑为调用**

`ensureLoginTab` 中原 `// 3. 平台特定：点击 QR 切换按钮...` 到 `// 4. 平台特定：QR 弹窗预激活...` 结束（`:144-193`）整段替换为：
```ts
  // 3. 平台 QR 激活（切换 + 预激活 + 等待可见）
  await activatePlatformQR(record.page, platform, config);
```
保留原 `// 5. 通用：QR 码过期检测与刷新`（`:196-247`）不动。

- [ ] **Step 5: 移除 `captureQR` 内 `qrActivationSelector` 激活分支**

`packages/browser-core/src/loginTabRegistry.ts:160-196`（从 `// 0. 如配置了激活选择器...` 到该 `if (config.qrActivationSelector) {...}` 块结束）整段删除。`captureQR` 保留：hostname 前置校验、QR 过期检测刷新（`:201-217`）、frames 收集、selector 循环截图、全页兜底。

- [ ] **Step 6: 运行测试确认通过**

Run: `cd /home/lrp/social_media_complete/apps/ts-api-gateway && npx vitest run src/services/__tests__/loginFlowHelpers.test.ts`
Expected: PASS

Run: `cd /home/lrp/social_media_complete/packages/browser-core && node --test src/__tests__/captureQRSelectors.test.ts`
Expected: PASS（`captureQRSelectors.test.ts` 若断言激活分支行为，需相应调整——读该测试文件确认；若它依赖 `qrActivationSelector` 在 captureQR 内点击，改为断言调用方已激活后的截图行为）。

- [ ] **Step 7: Commit**

```bash
git add apps/ts-api-gateway/src/services/loginFlowHelpers.ts packages/browser-core/src/loginTabRegistry.ts apps/ts-api-gateway/src/services/__tests__/loginFlowHelpers.test.ts packages/browser-core/src/__tests__/captureQRSelectors.test.ts
git commit -m "fix: 抽 activatePlatformQR 统一 CDP 激活，captureQR 不再重复激活"
```

---

## Task 4: A1+A2 — 强刷/F5 二次导航后激活 + timeout 收敛容错

**Files:**
- Modify: `apps/ts-api-gateway/src/services/wechatBotService.ts:785-848`（强制刷新 `:789` + F5 `:835`）

- [ ] **Step 1: 改强制刷新路径（`:785-801`）**

原 `:787-793`：
```ts
const { ensureLoginTab } = await import('./loginFlowHelpers');
const record = await ensureLoginTab(windowId, targetUserId, targetPlatform, targetFlowId);
if (record) {
  await record.page.goto(config.loginUrl, { waitUntil: 'domcontentloaded', timeout: 30000 });
  await record.page.waitForTimeout(3000);
  const qrBuf = await loginTabRegistry.captureQR(record.page, config);
  await botManager.sendLoginAlert(user.wechatUserid || userid, targetPlatform, targetUserId, qrBuf || undefined, targetFlowId);
  await botManager.sendTextMessage([userid], `🔄 已刷新 ${targetPlatform} 登录页，新二维码已发送`);
}
```
改为：
```ts
const { ensureLoginTab, activatePlatformQR } = await import('./loginFlowHelpers');
const record = await ensureLoginTab(windowId, targetUserId, targetPlatform, targetFlowId);
if (record) {
  // 二次导航冲掉了 ensureLoginTab 的 QR 激活，导航后必须重新激活
  try {
    await record.page.goto(config.loginUrl, { waitUntil: 'domcontentloaded', timeout: 12000 });
  } catch (err: any) {
    logger.warn({ targetPlatform, err: err.message }, '强制刷新 goto 超时，继续尝试激活 QR');
  }
  await record.page.waitForTimeout(3000);
  await activatePlatformQR(record.page, targetPlatform, config);
  const qrBuf = await loginTabRegistry.captureQR(record.page, config);
  await botManager.sendLoginAlert(user.wechatUserid || userid, targetPlatform, targetUserId, qrBuf || undefined, targetFlowId);
  await botManager.sendTextMessage([userid], `🔄 已刷新 ${targetPlatform} 登录页，新二维码已发送`);
}
```

- [ ] **Step 2: 改 F5 刷新路径（`:830-846`）**

原 `:832-839`：
```ts
const record = await ensureLoginTab(windowId, targetUserId, targetPlatform, targetFlowId);
if (record) {
  await record.page.reload({ waitUntil: 'domcontentloaded', timeout: 30000 });
  await record.page.waitForTimeout(3000);
  const qrBuf = await loginTabRegistry.captureQR(record.page, config);
  await botManager.sendLoginAlert(user.wechatUserid || userid, targetPlatform, targetUserId, qrBuf || undefined, targetFlowId);
  await botManager.sendTextMessage([userid], `♻️ 已F5刷新 ${targetPlatform} 页面，新二维码已发送`);
}
```
改为：
```ts
const { ensureLoginTab, activatePlatformQR } = await import('./loginFlowHelpers');
const record = await ensureLoginTab(windowId, targetUserId, targetPlatform, targetFlowId);
if (record) {
  try {
    await record.page.reload({ waitUntil: 'domcontentloaded', timeout: 12000 });
  } catch (err: any) {
    logger.warn({ targetPlatform, err: err.message }, 'F5 reload 超时，继续尝试激活 QR');
  }
  await record.page.waitForTimeout(3000);
  await activatePlatformQR(record.page, targetPlatform, config);
  const qrBuf = await loginTabRegistry.captureQR(record.page, config);
  await botManager.sendLoginAlert(user.wechatUserid || userid, targetPlatform, targetUserId, qrBuf || undefined, targetFlowId);
  await botManager.sendTextMessage([userid], `♻️ 已F5刷新 ${targetPlatform} 页面，新二维码已发送`);
}
```

- [ ] **Step 3: A1 补 `sendLoginQR:441` 短路分支激活**

`monitorService.ts:439-449` 原短路分支：
```ts
if (isOnLoginPage) {
  logger.info({ userId, platform, url: currentUrl }, `[${platform}] 当前页面已在登录域，直接用当前页截图`);
  const qrBuf = await loginTabRegistry.captureQR(page, config);
  ...
}
```
改为（`captureQR` 前先激活）：
```ts
if (isOnLoginPage) {
  logger.info({ userId, platform, url: currentUrl }, `[${platform}] 当前页面已在登录域，直接用当前页截图`);
  const { activatePlatformQR } = await import('./loginFlowHelpers');
  await activatePlatformQR(page, platform, config);
  const qrBuf = await loginTabRegistry.captureQR(page, config);
  ...
}
```

- [ ] **Step 4: 类型检查**

Run: `cd /home/lrp/social_media_complete && npx tsc -p apps/ts-api-gateway/tsconfig.json --noEmit`
Expected: 无新增错误。

- [ ] **Step 5: Commit**

```bash
git add apps/ts-api-gateway/src/services/wechatBotService.ts apps/ts-api-gateway/src/services/monitorService.ts
git commit -m "fix: 强刷/F5 二次导航后重新激活 QR + timeout 30s→12s 容错；sendLoginQR 短路分支补激活"
```

---

## Task 5: B1 — 监控成功后重置 active（dispatch 入口 + `markLoginRecovered`）

**Files:**
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts`（新增 `markLoginRecovered`，改 dispatch `:976-987`）

- [ ] **Step 1: 写失败测试 — `markLoginRecovered` 重置 active + 清 flowState**

在 `apps/ts-api-gateway/src/services/__tests__/` 新建或追加（参考既有测试 mock prisma 风格）。若该目录无 monitorService 测试基线，跳过单测，改用类型检查+集成验证（B1 逻辑简单）。**首选**：写一个纯函数测试 mock prisma + flowState。

```ts
// apps/ts-api-gateway/src/services/__tests__/markLoginRecovered.test.ts
import { describe, it, expect, jest, beforeEach } from 'vitest';
describe('markLoginRecovered (B1)', () => {
  beforeEach(() => { jest.resetModules(); });
  it('重置 status=active + cooldownUntil=0 + 清该平台所有 flowState', async () => {
    const updated: any[] = [];
    const deleted: string[] = [];
    jest.doMock('../../lib/prisma', () => ({
      prisma: { platformAccount: { update: async (args: any) => { updated.push(args); } } },
    }));
    jest.doMock('../loginFlowHelpers', () => ({ getFlowIdsForPlatform: () => ['creator'] }));
    jest.doMock('redis', () => ({}), { virtual: true });
    // getFlowState/delFlowState 在 monitorService 内用 redis；mock redis client
    const { markLoginRecovered } = await import('../monitorService');
    // 注：markLoginRecovered 是非导出函数；需在 monitorService.ts 中 export 它以供测试
    await markLoginRecovered(11, 'tencent');
    expect(updated).toHaveLength(1);
    expect(updated[0].where.id).toBe(11);
    expect(updated[0].data.status).toBe('active');
  });
});
```
> 若 `markLoginRecovered` 不便导出测试，则改为导出并在测试中 mock `delFlowState`/`getFlowIdsForPlatform`。如 mock 链路过深，可降级为：仅做类型检查 + Step 4 集成验证，不写单测（在 commit message 注明）。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /home/lrp/social_media_complete/apps/ts-api-gateway && npx vitest run src/services/__tests__/markLoginRecovered.test.ts`
Expected: FAIL（`markLoginRecovered` 未定义/未导出）

- [ ] **Step 3: 新增 `markLoginRecovered` 并 export**

在 `monitorService.ts` `delFlowState`（`:756`）附近新增：
```ts
/** 监控登录成功后重置账号为 active + 清该平台所有 flowState。供 dispatch 入口与 triggerLoginProbe 复用。 */
export async function markLoginRecovered(userId: number, platform: string): Promise<void> {
  const { prisma } = await import('../lib/prisma');
  await prisma.platformAccount.update({
    where: { id: userId },
    data: { status: 'active', cooldownUntil: BigInt(0) },
  });
  const { getFlowIdsForPlatform } = await import('./loginFlowHelpers');
  for (const fid of getFlowIdsForPlatform(platform)) {
    await delFlowState(userId, fid);
  }
}
```

- [ ] **Step 4: 改 dispatch 入口（`:976-987`）**

原：
```ts
switch (task.platform) {
  case 'douyin':
    return await runDouyinCheck(page, task, onProgress);
  case 'kuaishou':
    return await runKuaishouCheck(page, task, onProgress);
  case 'xiaohongshu':
    return await runXiaohongshuCheck(page, task, onProgress);
  case 'tencent':
    return await runTencentCheck(page, task, onProgress);
  default:
    throw new Error(`不支持的监控平台: ${task.platform}`);
}
```
改为：
```ts
switch (task.platform) {
  case 'douyin': {
    const r = await runDouyinCheck(page, task, onProgress);
    if (!r.riskDetected) await markLoginRecovered(task.userId, 'douyin');
    return r;
  }
  case 'kuaishou': {
    const r = await runKuaishouCheck(page, task, onProgress);
    if (!r.riskDetected) await markLoginRecovered(task.userId, 'kuaishou');
    return r;
  }
  case 'xiaohongshu': {
    const r = await runXiaohongshuCheck(page, task, onProgress);
    if (!r.riskDetected) await markLoginRecovered(task.userId, 'xiaohongshu');
    return r;
  }
  case 'tencent': {
    const r = await runTencentCheck(page, task, onProgress);
    if (!r.riskDetected) await markLoginRecovered(task.userId, 'tencent');
    return r;
  }
  default:
    throw new Error(`不支持的监控平台: ${task.platform}`);
}
```

- [ ] **Step 5: 运行测试 + 类型检查**

Run: `cd /home/lrp/social_media_complete/apps/ts-api-gateway && npx vitest run src/services/__tests__/markLoginRecovered.test.ts`
Expected: PASS（或若降级无单测，跳过）
Run: `cd /home/lrp/social_media_complete && npx tsc -p apps/ts-api-gateway/tsconfig.json --noEmit`
Expected: 无新增错误。

- [ ] **Step 6: Commit**

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts apps/ts-api-gateway/src/services/__tests__/markLoginRecovered.test.ts
git commit -m "fix: 监控成功后 markLoginRecovered 重置 active + 清 flowState"
```

---

## Task 6: B2 — `triggerLoginProbe` 在 find null 时回退 `ensureLoginTab` 检测

**Files:**
- Modify: `apps/ts-api-gateway/src/services/monitorService.ts:796-843`（`triggerLoginProbe` 内 setTimeout 回调）

- [ ] **Step 1: 改 `:801-823` 的 find null 分支与 logged_in 分支**

原 `:801-823`：
```ts
const record = await loginTabRegistry.find(windowId, platform, fid, browser, config.domain);
if (!record) {
  await delFlowState(userId, fid);
  return;
}

const result = await loginTabRegistry.checkLoginState(record.page, config);
if (result === 'logged_in') {
  if (config.closeOnLoginSuccess) {
    await loginTabRegistry.closeLoginTab(windowId, platform, fid);
  } else {
    await loginTabRegistry.unregister(windowId, platform, fid);
  }
  await delFlowState(userId, fid);

  const allStates = await getAllFlowStates(userId, platform);
  if (allStates.size === 0) {
    const { prisma } = await import('../lib/prisma');
    await prisma.platformAccount.update({
      where: { id: userId },
      data: { status: 'active', cooldownUntil: BigInt(0) },
    });
  }
} else {
  ...原 :824-838 冷却逻辑...
}
```
改为：
```ts
const loginHost = getLoginHost(config.loginUrl, config.domain);
let record = await loginTabRegistry.find(windowId, platform, fid, browser, loginHost);
if (!record) {
  // find null（无残留登录 tab）→ 回退 ensureLoginTab 打开/复用登录页再检测
  const { ensureLoginTab } = await import('./loginFlowHelpers');
  record = await ensureLoginTab(windowId, userId, platform, fid);
  if (!record) { await delFlowState(userId, fid); return; }
}

const result = await loginTabRegistry.checkLoginState(record.page, config);
if (result === 'logged_in') {
  if (config.closeOnLoginSuccess) {
    await loginTabRegistry.closeLoginTab(windowId, platform, fid);
  } else {
    await loginTabRegistry.unregister(windowId, platform, fid);
  }
  await markLoginRecovered(userId, platform);   // 复用 B1 helper（重置 active + 清所有 flowState）
} else {
  // 未登录 → 发 QR + 设 flowState 冷却（原 :824-838 逻辑保留）
  await sendLoginQR(record.page, userId, platform, fid);   // sendLoginQR 是本文件 :415 顶层导出，直接调用
  const newLevel = Math.min((state.cooldownLevel || 0) + 1, 4);
  const cooldownsMs = [30, 60, 120, 240, 240];
  const cooldownMs = cooldownsMs[newLevel] * 60 * 1000;
  await setFlowState(userId, fid, {
    status: 'login_required',
    cooldownLevel: newLevel,
    cooldownUntil: Date.now() + cooldownMs,
    lastProbeAt: Date.now(),
  });
  const next = setTimeout(() => {
    triggerLoginProbe(userId, platform, windowId, fid).catch(() => {});
  }, cooldownMs);
  next.unref();
}
```
> 注意：原 logged_in 分支用 `getAllFlowStates` 判断"全部 flowId 都登录恢复"才重置 active；现 `markLoginRecovered` 直接清该平台所有 flowState 并重置 active，语义更直接（probe 单 flowId 命中 logged_in 即认为该平台恢复）。`state` 变量在 `:793` 由 `getFlowState` 取得，else 分支 `state.cooldownLevel` 仍可用（find null 回退 ensureLoginTab 路径下 `state` 可能 null——需在 else 分支前判 `state`，若 null 用 `cooldownLevel: 0`）。补防御：`const newLevel = Math.min((state?.cooldownLevel || 0) + 1, 4);`

- [ ] **Step 2: 顶部 import 补 `getLoginHost`（若 Task 2 未在此文件 import）**

确认 `monitorService.ts` 顶部已 import `getLoginHost`（Task 2 Step 3 已加）。若未加，补 `import { getLoginHost } from '@social-media/browser-core';` 或从 `./loginFlowHelpers` re-export。

- [ ] **Step 3: 类型检查**

Run: `cd /home/lrp/social_media_complete && npx tsc -p apps/ts-api-gateway/tsconfig.json --noEmit`
Expected: 无新增错误。

- [ ] **Step 4: Commit**

```bash
git add apps/ts-api-gateway/src/services/monitorService.ts
git commit -m "fix: triggerLoginProbe find null 时回退 ensureLoginTab 检测，未登录则发 QR"
```

---

## Task 7: 全量回归 + 验收

**Files:** 无代码改动，仅验证。

- [ ] **Step 1: 全量类型检查**

Run: `cd /home/lrp/social_media_complete && npx tsc -p apps/ts-api-gateway/tsconfig.json --noEmit && (cd packages/browser-core && npx tsc --noEmit)`
Expected: 无错误。

- [ ] **Step 2: 全量测试**

Run: `cd /home/lrp/social_media_complete/packages/browser-core && node --test src/__tests__/*.test.ts`
Run: `cd /home/lrp/social_media_complete/apps/ts-api-gateway && npx vitest run`
Expected: 全部 PASS。

- [ ] **Step 3: 重建容器 + 实测验收**

```bash
cd /home/lrp/social_media_complete
docker compose up -d --build ts-api-gateway
docker logs sm-ts-api -f
```
验收（按 spec §6）：
1. **#1a**：连续两次企微发 `强制刷新 11 kuaishou creator`，日志第二次出现 `[ensureLoginTab] reused existing platform tab`，无 `openLoginTab: navigating`；浏览器标签页数不增加。重启容器后快手已开登录 tab 仍能被 `find` 枚举命中。
2. **#1b**：`强制刷新`/`F5刷新 11 kuaishou creator` 后企微收到 QR 图片；日志出现 `activatePlatformQR` + `captureQR: captured via "img[alt=\"qrcode\"]"`；F5 不再 30s 超时。
3. **#2②**：视频号 `login_required` 时前端点"更新"，监控成功后卡片 `status` 变 `active`，"需重新登录"消失。
4. **#2①**：视频号无残留登录 tab 时点"恢复登录"，probe 回退打开登录页检测；已登录则 active，未登录则发 QR。
5. 回归：抖音/视频号/小红书刷新与监控链路正常。

- [ ] **Step 4: 最终 Commit（如有验收修复）**

```bash
git add -A
git commit -m "test: 全量回归验证通过"
```

---

## 自审

**Spec 覆盖：**
- A0（find loginHost + 内存校验 + 3 调用点 + 复用分支）→ Task 1 + Task 2 ✓
- A1（activatePlatformQR + captureQR 移除激活 + ensureLoginTab/强刷/F5/sendLoginQR 短路 四处激活）→ Task 3 + Task 4 Step 3 ✓
- A2（goto/reload 30s→12s + try/catch）→ Task 4 Step 1-2 ✓
- A3（删除，兜底保留）→ 不涉及代码，无 Task ✓
- B1（markLoginRecovered + dispatch）→ Task 5 ✓
- B2（probe find null 回退）→ Task 6 ✓
- 验收 → Task 7 ✓

**占位符扫描：** Task 5 Step 1 标注了 mock 链路过深时的降级路径（不写单测，commit 注明）——这是明确的降级而非占位符。其余步骤均含完整代码。

**类型/签名一致性：**
- `getLoginHost(loginUrl, fallbackDomain)` — Task 1 定义，Task 2/6 调用一致 ✓
- `isOnLoginDomain(url, loginHost)` — Task 1 定义，Task 2 复用分支调用一致 ✓
- `activatePlatformQR(page, platform, config): Promise<boolean>` — Task 3 定义，Task 3/4 调用一致 ✓
- `markLoginRecovered(userId, platform)` — Task 5 定义并 export，Task 6 调用一致 ✓
- `find(windowId, platform, flowId, browser, loginHost)` — Task 1 改签名，Task 2 三处调用一致 ✓
