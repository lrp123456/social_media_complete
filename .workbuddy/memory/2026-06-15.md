# 2026-06-15 工作日志

## 任务: 调研快手/抖音发布流程 + 评论回复功能修复

### 代码位置
- 发布器: `apps/ts-api-gateway/src/platforms/{kuaishou,douyin,xiaohongshu,...}.ts`
- 爬虫(评论拉取+回复): `apps/ts-api-gateway/src/crawlers/{kuaishouCrawler,douyinCrawler}.ts`
  - kuaishouCrawler.ts: 2160 行, `replyToComment` 在 2004 行
  - douyinCrawler.ts: 3345 行, `replyToComment` 在 2852 行, `scrollExpandAndFindTarget` 在 2958 行
- 选择器配置: `data/selectors.json` (2671 行)
- 浏览器底层: `packages/browser-core/src/humanActions.ts`

### 关键发现 - 代码 Bug
1. **抖音回复 Bug**: `douyinCrawler.ts:2996-3000, 3107-3111, 3180-3184, 3310-3314` 的内联时间提取逻辑有 bug — 抖音创作者中心时间格式是"12-25 14:30"，但正则首先要求"YYYY-MM-DD HH:MM"，所以基本永远匹配不到。`/发布于\s*(\d{1,2})[\/\-.](\d{1,2})/g` 也只在年内。
2. **抖音回复 Bug**: 2903-2906 行输入框选择器用了花引号 `[class*=”reply-content”]`，这不是合法的 CSS 属性选择器语法，浏览器直接忽略，永远不会命中。
3. **快手回复 Bug**: 2111 行 `HumanActions.safeCDPType(page, replyText)` 缺 selector 参数 — `safeCDPType` 默认行为是聚焦到当前活动元素，逻辑上"恰好"对，但容易误输入到错误的元素。
4. **快手回复子评论未支持**: 没有子评论展开 + 定位子评论的代码，子评论回复大概率失败。
5. **抖音回复子评论**: 2958 行的 `scrollExpandAndFindTarget` 有 isSub 分支，但被时间提取 bug 拖累无法工作。

### 项目架构关键点
- 所有浏览器操作必须通过 `HumanActions.*` 静态方法 (贝塞尔曲线 + 高斯偏移)
- 选择器通过 `SelectorReader` 从 `data/selectors.json` 读
- TS 端包管理: pnpm 9.15.0, Node 20+
- 使用 patchright 替代 playwright (反检测)
- 发布器继承 `BasePublisher` (455 行), 已实现 `submitPublishWithFlowRules` 走 selectors.json flowRules

### 任务流程 (用户指定)
调研代码 + 浏览器实操确认代码修复方案 + 修复代码 + 代码验证（通过真实的回复，包括根评论回复和子评论回复），验证通过则任务完成，验证失败则重复调研浏览器+修复代码+验证，直到成功
